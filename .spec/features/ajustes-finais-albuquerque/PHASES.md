# Phases: ajustes-finais-albuquerque

Gerado por /plan a partir de PLAN.md — view executável para `./ralph.sh .spec/features/ajustes-finais-albuquerque/PHASES.md`.

Ordem RIGID (SPEC §47): Etapa 1 auditoria → Etapa 2 administração de usuários → Etapa 3 primeiro acesso e senha → Etapa 4 e-mail → Etapa 5 design system → Etapa 6 identidade global → Etapa 7 responsividade → Etapa 8 testes/regressão → Etapa 9 LOGOS (última etapa de implementação) → Etapa 10 preparação para deploy. Etapa 11 (domínio) é diferida — sem tarefa neste arquivo (ver PLAN.md "Deferred stage"). Regras transversais em toda fase: não alterar `app/Actions/Pedidos/*`, `app/Domain/*`, `PedidoPolicy`, `PedidoEventPolicy`, `LoginForm`, enums, migrations existentes; nenhuma migration nova (CT-05); nenhum segredo em arquivo versionado; `vendor/bin/pint --dirty --format agent` antes de encerrar; logos NUNCA antes da Phase 10; e-mails síncronos (sem queue); nunca `migrate:fresh`.

## Phase 1: Etapa 1 — Auditoria e baseline

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T01 — Etapa 1: baseline audit and preflight
      Arquivos: `.spec/features/ajustes-finais-albuquerque/audit-etapa1.md` (novo; nenhum código de aplicação)
      Mudança: preflight Git (§40: branch `build/v0-demo-laravel`, `git fetch`, HEAD descende de `82e4d48`); rodar `php artisan test --compact` e `npm run build` e registrar baseline (esperado 296 testes / 854 assertions / 0 falhas, build exit 0); registrar com evidência `arquivo:linha` os 11 itens da Etapa 1 (autenticação `LoginForm.php:47`, users/profiles, roles (3), Policies/Gates `AppServiceProvider.php:29-31`, obras, obra_profile, mail config `config/mail.php:17` + `config/services.php:21-23` + `phpunit.xml:36`, views/layouts, Tailwind `app.css:6` + `sky-*` em 14 arquivos, 6 componentes, lista de testes); confirmar via `composer show -a resend/resend-php` a versão `^1.x` compatível com laravel/framework (registrar; NÃO instalar); confirmar existência/nome/dimensões dos dois originais em `/mnt/c/.../logo_Albuquerque.png` (1063×345, 209281 B) e `logo_MC.png` (1305×200, 23177 B) SEM copiar.
      Cobre: RF-01, RF-26, UI-19, RNF-13, RNF-18
      Acceptance criteria: `audit-etapa1.md` existe com os 11 itens + evidências, contagens da suíte e do build, versão do SDK Resend registrada e verificação das logos; `git status` não mostra nenhum arquivo de aplicação alterado; `ls public/images` não existe (nenhuma cópia feita).
      Testes: suíte existente inalterada — `php artisan test --compact` verde (baseline)

## Phase 2: Etapa 2a — Administração de usuários: autorização, actions, middleware e comando

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T02 — Gate `manage-users`, `UserPolicy` and factory states
      Arquivos: `app/Providers/AppServiceProvider.php`, `app/Policies/UserPolicy.php` (novo via `make:policy UserPolicy --model=User`), `database/factories/UserFactory.php`, `tests/Feature/Authorization/UserPolicyTest.php` (novo), `tests/Feature/Authorization/RoleGatesTest.php` (apenas acrescentar)
      Mudança: `Gate::define('manage-users', fn (User $user): bool => $user->role?->slug === RoleSlug::Gestao->value)` ao lado dos 3 gates existentes; `UserPolicy` com `viewAny/create/update/changeRole/activate/deactivate/sendAccessLink` resolvendo por `Gate::allows('manage-users')` (só o gate nomeia o papel — RNF-11); `changeRole`/`deactivate` retornam false quando `$actor->is($target)`; `UserFactory::inactive()` (`is_active => false`). Não tocar em `PedidoPolicy`/`PedidoEventPolicy`.
      Cobre: RF-02, RF-05, RF-30, RNF-10, RNF-11
      Acceptance criteria: `Gate::allows('manage-users')` é true só para gestao; `UserPolicy` existe com as 7 abilities e nega tudo para obra/suprimentos e nega `changeRole`/`deactivate` para a própria conta; `RoleSlug::cases()` continua com 3 casos; `User::factory()->inactive()` existe.
      Testes: `tests/Feature/Authorization/UserPolicyTest.php` — gestao permitido em todas as abilities; obra/suprimentos negados; self negado em changeRole/deactivate; `RoleGatesTest` — manage-users só gestao
- [ ] T03 — User administration Actions with lockout guards
      Arquivos: `app/Actions/Usuarios/CreateUserAction.php`, `app/Actions/Usuarios/UpdateUserAction.php`, `app/Actions/Usuarios/SetUserActiveAction.php`, `app/Actions/Usuarios/Concerns/GuardsUserAdministration.php`, `app/Actions/Usuarios/Concerns/GuardsGestaoLockout.php` (novos, formato de `app/Actions/Pedidos/*`), `tests/Feature/Actions/Usuarios/{CreateUserAction,UpdateUserAction,SetUserActiveAction,GestaoLockoutGuard}Test.php` (novos)
      Mudança: `GuardsUserAdministration::ensureActorManagesUsers()` → `AuthorizationException` PT-BR quando o ator não passa em `manage-users`; `GuardsGestaoLockout::ensureNotSelf()` e `ensureAnotherActiveGestaoRemains()` → `ValidationException` PT-BR (RF-30). `CreateUserAction::execute(User $actor, array $data): array{user, invite_sent}`: `Validator::make` PT-BR (name obrigatório; email `unique:users`; role_id `exists:roles`; `obra_ids` obrigatório ≥1 quando role obra, proibido caso contrário); `DB::transaction` com `password => Str::password(32)` (aleatória, hashed pelo cast; nunca default), `is_active=true`, `is_demo=false`, `obras()->sync`; retorna `invite_sent=false` (envio pós-commit entra em T08). `UpdateUserAction::execute(User $actor, User $target, array $data): User`: valida (email unique ignorando target); troca de perfil → `ensureNotSelf` + última-Gestão; em 1 transação atualiza name/email/role_id, `sync` obras (≥1) quando obra, `detach()` quando deixa de ser obra (Q-10.2); troca de e-mail só atualiza (sem notificação, sem apagar tokens — Q-10.3). `SetUserActiveAction::execute(User $actor, User $target, bool $active): User`: desativação passa por ambas as guardas; só `update(['is_active' => $active])` — nunca apaga users/pedidos/pedido_events/obra_profile/sessions. Sem `PedidoEvent`. Sem migration (CT-05).
      Cobre: RF-06, RF-07, RF-08, RF-09, RF-10, RF-11, RF-18, RF-30, RNF-02, RNF-10, CT-05
      Acceptance criteria: as 3 Actions e 2 traits existem com assinaturas tipadas e PHPDoc de array shape; criar usuário gera linha com `is_active=true`, `is_demo=false`, pivot exato, senha aleatória hashed (login com `password` falha); obra com 0 obras é recusado em create e edit; troca obra→suprimentos deixa 0 linhas em `obra_profile`; troca de e-mail não envia notificação e preserva token antigo; desativar não apaga nenhuma linha relacionada; auto-desativação/auto-troca de perfil e remoção da última Gestão ativa são recusadas com mensagem PT-BR; `ls database/migrations | wc -l` = 14.
      Testes: `CreateUserActionTest` (TC-04, TC-23, RF-07, ator obra/suprimentos → AuthorizationException), `UpdateUserActionTest` (TC-05, TC-06, TC-23, TC-24, `PedidoPolicy::view` reflete novas obras), `SetUserActiveActionTest` (TC-07, TC-09, RF-11), `GestaoLockoutGuardTest` (TC-21, TC-22)
- [ ] T04 — `EnsureUserIsActive` middleware on the authenticated group
      Arquivos: `app/Http/Middleware/EnsureUserIsActive.php` (novo via `make:middleware`), `bootstrap/app.php`, `routes/web.php`, `app/Providers/AppServiceProvider.php`, `tests/Feature/Auth/EnsureUserIsActiveTest.php` (novo)
      Mudança: `handle()` — se `$request->user()?->is_active === false`: `Auth::guard('web')->logout()`, `session()->invalidate()`, `session()->regenerateToken()`, `redirect()->route('login')->with('status', 'Sua conta foi desativada. Fale com a Gestão.')`; alias `'active' => EnsureUserIsActive::class` no bloco `alias` existente de `bootstrap/app.php`; grupo autenticado passa a `Route::middleware(['auth', 'active'])` sem tocar em `LoginForm`; registrar como middleware persistente do Livewire (verificar API Livewire 4 com search-docs) para cobrir `/livewire/update`. Linhas de `sessions` não são apagadas.
      Cobre: RF-31, RF-10, RNF-10, Q-06
      Acceptance criteria: usuário logado e depois desativado é redirecionado para `route('login')` com a mensagem na próxima requisição autenticada e `Auth::check()` fica false; chamada Livewire após desativação também é cortada; usuário ativo não é afetado; `bootstrap/app.php` contém o alias `active`; `routes/web.php` aplica `['auth', 'active']`; `tests/Feature/Auth/LoginTest.php` e `UnauthenticatedAccessTest.php` inalterados e verdes.
      Testes: `tests/Feature/Auth/EnsureUserIsActiveTest.php` — TC-20 (GET e Livewire call após desativação; caminho ativo intacto)
- [ ] T05 — Bootstrap command `users:create-gestao`
      Arquivos: `app/Console/Commands/CreateGestaoUser.php` (novo via `make:command`), `tests/Feature/Console/CreateGestaoUserCommandTest.php` (novo)
      Mudança: assinatura `users:create-gestao {--name=} {--email=} {--password=} {--reset-password}` sem valores default; senha lida de `--password` ou, se ausente, de `env('GESTAO_BOOTSTRAP_PASSWORD')` dentro de `handle()` (nunca em `config/`); e-mail inválido/ausente ou senha ausente → mensagem de uso PT-BR e `self::INVALID`, sem criar nada; upsert idempotente por e-mail em `DB::transaction` (`firstOrNew`): novo → name, role gestao, `is_active=true`, `is_demo=false`, senha (hashed pelo cast); existente → role gestao, `is_active=true`, `is_demo=false`, name se informado, senha SOMENTE com `--reset-password`; saída "Usuário Gestão garantido: {email} (criado|atualizado)" — nunca a senha nem o hash.
      Cobre: RF-32, RF-33, CT-08, RNF-02, RNF-07, Q-01
      Acceptance criteria: rodar duas vezes com o mesmo e-mail deixa exatamente 1 linha gestao ativa não-demo; sem `--reset-password` o hash não muda; com a flag `Hash::check(nova)` é true; a saída nunca contém a senha; sem senha ou sem e-mail o comando retorna código ≠ 0 e não cria linha; `grep -n "password" app/Console/Commands/CreateGestaoUser.php` não mostra nenhum valor literal de senha.
      Testes: `tests/Feature/Console/CreateGestaoUserCommandTest.php` — TC-18 (idempotência, não duplica, não sobrescreve sem flag, sobrescreve com flag, `doesntExpectOutputToContain($password)`, falha sem senha/e-mail)

## Phase 3: Etapa 2b — Administração de usuários: interface Gestão

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T06 — Users area: routes, Livewire pages, nav entry
      Arquivos: `routes/web.php`, `app/Livewire/Gestao/Usuarios/Index.php`, `app/Livewire/Gestao/Usuarios/Form.php` (novos via `make:livewire`), `resources/views/livewire/gestao/usuarios/index.blade.php`, `resources/views/livewire/gestao/usuarios/form.blade.php` (novos), `resources/views/layouts/app.blade.php` (só o item de nav), `tests/Feature/Livewire/UsuariosIndexTest.php`, `tests/Feature/Livewire/UsuariosFormTest.php` (novos)
      Mudança: dentro do grupo `can:is-gestao` + `prefix('gestao')` + `name('gestao.')`, subgrupo `can:manage-users` com `GET /usuarios` → `Index` (`gestao.usuarios.index`), `GET /usuarios/novo` → `Form` (`gestao.usuarios.create`), `GET /usuarios/{user}/editar` → `Form` (`gestao.usuarios.edit`). `Index` (`#[Layout('layouts.app')]`, `WithPagination`, `$search`, `updating()` reseta página, convenções de `Gestao\TodosPedidos`): `mount()` → `authorize('viewAny', User::class)`; listagem `with(['role','obras'])` filtrada por nome OU e-mail case-insensitive (`lower()`/`ilike`); colunas nome, e-mail, perfil, status (badge Ativo/Inativo), obras (só perfil obra); ações `Editar`, `Ativar`/`Desativar` (`setActive(int $userId, bool $active, SetUserActiveAction $action)` com `authorize` + delegação; `ValidationException` exibida inline). `Form` cria e edita (`mount(?User $user = null)` com `authorize('create'|'update')`; seletor de obras só quando o perfil selecionado é obra; `save()` re-autoriza e delega a `CreateUserAction`/`UpdateUserAction`; redirect ao index com flash; nunca renderiza senha). Adicionar `['label' => 'Usuários', 'route' => 'gestao.usuarios.index', 'active' => 'gestao.usuarios.*']` como 4º item do ramo gestao de `$navItems`. Usar só `.card`, `.data-table`, `.btn-primary`, `.btn-secondary`, `.badge`, `.form-control`.
      Cobre: RF-03, RF-04, RF-05, RF-06, RF-07, RF-08, RF-09, RF-10, RF-11, RF-25, CT-01, UI-08, UI-23, RNF-03, RNF-10
      Acceptance criteria: `php artisan route:list --name=gestao.usuarios` mostra as 3 rotas com `auth`, `active`, `can:is-gestao`, `can:manage-users`; gestao vê o link `Usuários` e a listagem com as 5 colunas (usuário obra com 2 obras mostra as duas; suprimentos/gestao sem obras); busca `ana`/`BRUNO@`/vazio filtra conforme RF-04; obra e suprimentos recebem 403 na rota e em chamadas Livewire forjadas (`setActive`, `save`) sem alterar `users`; visitante é redirecionado para `route('login')`; nenhum HTML renderizado contém hash de senha; `layouts/app.blade.php` tem exatamente 4 itens no ramo gestao.
      Testes: `UsuariosIndexTest` (TC-01, TC-02, TC-03, TC-15, RF-04, forged calls), `UsuariosFormTest` (create/edit via componente, mensagens por campo, seletor de obras oculto para não-obra, forged save → 403)

## Phase 4: Etapa 3 — Primeiro acesso e senha

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T07 — `passwords.invites` broker and PT-BR notifications
      Arquivos: `config/auth.php`, `app/Notifications/FirstAccessInvite.php`, `app/Notifications/ResetPasswordPtBr.php` (novos via `make:notification`), `app/Models/User.php` (apenas `sendPasswordResetNotification()`), `tests/Feature/Notifications/AuthNotificationsTest.php` (novo)
      Mudança: adicionar `'invites' => ['provider' => 'users', 'table' => env('AUTH_PASSWORD_RESET_TOKEN_TABLE', 'password_reset_tokens'), 'expire' => 4320, 'throttle' => 60]` em `passwords` (mesma tabela, sem migration). `FirstAccessInvite` (canal mail, NÃO `ShouldQueue`): assunto PT-BR com `config('app.name')`, saudação pelo nome, botão "Definir minha senha" → `route('invite.show', ['token' => ..., 'email' => ...])`, frase "válido por 72 horas" derivada de `config('auth.passwords.invites.expire')`, sem senha, sem assinatura MC. `ResetPasswordPtBr extends Illuminate\Auth\Notifications\ResetPassword`: `toMail()` PT-BR, botão "Redefinir senha" → `route('password.reset', [...])`, validade 60 minutos de `config('auth.passwords.users.expire')`. `User::sendPasswordResetNotification()` → `ResetPasswordPtBr`.
      Cobre: RF-15, RF-27, CT-03, CT-07, RNF-01, RNF-08, RNF-12, Q-02
      Acceptance criteria: `config('auth.passwords.invites')` retorna expire 4320, throttle 60, tabela `password_reset_tokens`; as duas notificações existem, não implementam `ShouldQueue`, geram links cujo host é o de `config('app.url')` (muda ao mudar `app.url`), contêm token e e-mail, não contêm senha nem "MC Inteligência"; textos em PT-BR com "72 horas" e "60 minutos" respectivamente.
      Testes: `tests/Feature/Notifications/AuthNotificationsTest.php` — config do broker, render de `toMail()` das duas classes (PT-BR, host = APP_URL, sem senha, sem MC)
- [ ] T08 — `SendAccessLinkAction`, post-commit invite on create, Gestão resend action
      Arquivos: `app/Actions/Usuarios/SendAccessLinkAction.php` (novo), `app/Actions/Usuarios/CreateUserAction.php`, `app/Livewire/Gestao/Usuarios/Index.php`, `app/Livewire/Gestao/Usuarios/Form.php`, `resources/views/livewire/gestao/usuarios/index.blade.php`, `tests/Feature/Actions/Usuarios/SendAccessLinkActionTest.php` (novo), `tests/Feature/Livewire/UsuariosFormTest.php`, `tests/Feature/Livewire/UsuariosIndexTest.php` (estender)
      Mudança: `SendAccessLinkAction::execute(User $actor, User $target): string` — guarda de ator; `Password::broker('invites')->sendResetLink(['email' => $target->email], fn ($user, $token) => $user->notify(new FirstAccessInvite($token)))`; retorna o status; nunca lê `users.password`. `CreateUserAction`: após o `DB::transaction` retornar, chamar a action em `try/catch (\Throwable)` com `report($e)` e `invite_sent=false` — nunca dentro da transação. `Form::save()` mostra "Usuário criado. Convite enviado para {email}." ou "Usuário criado, mas o convite não pôde ser enviado — use Reenviar convite.". `Index::sendAccessLink(int $userId, SendAccessLinkAction $action)` → `authorize('sendAccessLink', $user)`; `RESET_THROTTLED` → "Um link já foi enviado para este e-mail há menos de 1 minuto. Aguarde para reenviar."; `RESET_LINK_SENT` → "Link de acesso enviado para {email}."; outros → erro genérico. Botão secundário por linha "Reenviar convite / Enviar link de redefinição".
      Cobre: RF-06, RF-14, RF-15, RF-29, RNF-05, RNF-08, CT-07, Q-04, Q-05
      Acceptance criteria: criar usuário envia exatamente 1 `FirstAccessInvite` ao e-mail do usuário e cria linha em `password_reset_tokens`; com o envio lançando exceção o usuário permanece criado, a exceção é reportada e a resposta mostra o feedback honesto sem exceção não tratada; falha de validação → nenhuma notificação; reenvio duas vezes em 60 s envia 1 notificação e a segunda resposta contém a mensagem explícita de "já foi enviado"; obra/suprimentos → 403/AuthorizationException; nenhuma resposta, log ou e-mail contém senha em texto puro.
      Testes: `SendAccessLinkActionTest` (envio, token, throttled = 1 envio + status, ator obra negado), `UsuariosFormTest` (TC-16 envio após create; TC-19 falha simulada; rollback → `assertNothingSent`), `UsuariosIndexTest` (TC-25 lado Gestão; forged call → 403)
- [ ] T09 — "Esqueci minha senha" page (`password.request`)
      Arquivos: `routes/web.php`, `app/Livewire/Auth/ForgotPassword.php`, `resources/views/livewire/auth/forgot-password.blade.php` (novos), `tests/Feature/Auth/PasswordRecoveryRequestTest.php` (novo)
      Mudança: no grupo `guest`, `Route::get('/esqueci-senha', ForgotPassword::class)->name('password.request')` (caminho FLEXIBLE, nome RIGID). Componente `#[Layout('auth.login')]` no formato de `LoginForm` (rules/messages PT-BR, `role="alert"`): `$email`, `$sent`; `sendResetLink()` valida e-mail, chama `Password::broker('users')->sendResetLink(['email' => $this->email, 'is_active' => true])`, ignora o status e define `$sent = true`; a view mostra sempre o mesmo texto "Se o e-mail informado estiver cadastrado e ativo, você receberá um link para redefinir a senha em instantes." + link "Voltar ao login". Opcional FLEXIBLE: `throttle:6,1` na rota.
      Cobre: RF-19, RF-20, RF-21, RF-24, RNF-03, RNF-04, RNF-05, CT-02, UI-24
      Acceptance criteria: `route('password.request')` existe sob `guest` e renderiza input de e-mail; e-mail ativo → linha em `password_reset_tokens` + `ResetPasswordPtBr` enviada + texto genérico; e-mail desconhecido, e-mail de usuário inativo e reenvio throttled produzem HTML e status idênticos ao caso ativo, sem notificação para desconhecido/inativo; dois envios rápidos para o mesmo ativo geram exatamente 1 notificação; funciona para obra, suprimentos e gestao.
      Testes: `tests/Feature/Auth/PasswordRecoveryRequestTest.php` — TC-10, TC-15, TC-25 (lado público), RF-21 (inativo/desconhecido/throttled byte-idênticos), RF-24 dataset de 3 papéis
- [ ] T10 — Reset password page (`password.reset`)
      Arquivos: `routes/web.php`, `app/Livewire/Auth/ResetPassword.php`, `resources/views/livewire/auth/reset-password.blade.php` (novos), `tests/Feature/Auth/PasswordResetTest.php` (novo)
      Mudança: `Route::get('/redefinir-senha/{token}', ResetPassword::class)->name('password.reset')` no grupo `guest`. Componente `#[Layout('auth.login')]`: `mount(string $token)` lê token e `email` da query; `resetPassword()` valida `email` e `password` `['required', 'confirmed', Password::defaults()]` com mensagens PT-BR; `Password::broker('users')->reset([...], fn (User $user, string $password) => $user->forceFill(['password' => $password, 'remember_token' => Str::random(60)])->save() + event(new PasswordReset($user)))`; sucesso → `redirect()->route('login')->with('status', 'Senha redefinida. Entre com a nova senha.')`; qualquer outro status → erro único genérico "Este link é inválido ou expirou. Solicite um novo.".
      Cobre: RF-22, RF-23, RF-24, RNF-01, RNF-02, RNF-03, RNF-06, CT-02, UI-24
      Acceptance criteria: token válido → `Hash::check(nova)` true, login com a nova senha funciona e a antiga falha com 'E-mail ou senha inválidos.', token removido e reutilização falha; token adulterado → erro genérico e hash inalterado; token com mais de 60 minutos → idem; senha < 8 ou confirmação divergente → erros de campo; válido para os 3 papéis.
      Testes: `tests/Feature/Auth/PasswordResetTest.php` — TC-11, TC-12 (`travel(61)->minutes()`), TC-13, TC-14, RF-23, RF-24
- [ ] T11 — First-access invite page (`invite.show`)
      Arquivos: `routes/web.php`, `app/Livewire/Auth/AcceptInvite.php`, `resources/views/livewire/auth/accept-invite.blade.php` (novos), `tests/Feature/Auth/FirstAccessInviteTest.php` (novo)
      Mudança: `Route::get('/primeiro-acesso/{token}', AcceptInvite::class)->name('invite.show')` no grupo `guest`. Mesmo formato de `ResetPassword`, mas `Password::broker('invites')` e copy de primeiro acesso ("Defina sua senha", "Bem-vindo(a) ao {{ config('app.name') }}", botão "Definir senha"); sucesso → login com "Senha definida. Entre com seu e-mail e a nova senha."; falha → "Este link é inválido ou expirou. Peça um novo convite à Gestão.". Regras de senha compartilhadas com `ResetPassword` (trait pequeno ou constantes — FLEXIBLE).
      Cobre: RF-16, RF-17, RF-18, RNF-01, RNF-06, CT-03, UI-24, Q-02
      Acceptance criteria: usuário criado por `CreateUserAction` recebe link com token, abre `invite.show`, define senha → `Hash::check` true, token invalidado, `LoginForm` autentica; token adulterado ou já consumido → erro genérico sem revelar existência do e-mail; `travel(73)->hours()` expira e `travel(71)->hours()` ainda funciona; nenhuma resposta contém senha.
      Testes: `tests/Feature/Auth/FirstAccessInviteTest.php` — TC-16, TC-11, TC-12 (72 h: 71 h ok / 73 h expirado), reuso de token
- [ ] T12 — Login link "Esqueci minha senha", CSRF coverage of new forms
      Arquivos: `resources/views/livewire/auth/login-form.blade.php`, `tests/Feature/Security/CsrfProtectionTest.php` (estender), `tests/Feature/Livewire/LoginFormTest.php` (novo teste apenas)
      Mudança: adicionar `<a href="{{ route('password.request') }}">Esqueci minha senha</a>` sob o campo senha, sem tocar em `LoginForm.php`; estender `CsrfProtectionTest` (`app['env'] = 'production'`) com POST de visitante em `default-livewire.update` sem token → 419 e com token → não 419; `password.request` e `invite.show` renderizam meta `csrf-token`.
      Cobre: RF-19, RNF-03, UI-15
      Acceptance criteria: `GET /login` contém o texto "Esqueci minha senha" apontando para `route('password.request')`; `app/Livewire/Auth/LoginForm.php` byte-idêntico a `82e4d48`; os testes existentes de `CsrfProtectionTest` e `LoginFormTest` permanecem intactos e verdes com os novos casos adicionados.
      Testes: `LoginFormTest` (link presente), `CsrfProtectionTest` (novos casos para os formulários de autenticação)

## Phase 5: Etapa 4 — E-mail

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T13 — Resend SDK dependency (approved) and transport wiring check
      Arquivos: `composer.json`, `composer.lock`, `tests/Feature/Compliance/MailTransportTest.php` (novo)
      Mudança: `composer require resend/resend-php:^1.15 --no-interaction` (única dependência aprovada — Q-03; compatível: framework v13.32.0 `require-dev ^1.0`, guzzle 8 já instalado); commitar `composer.lock`; conferir que `config/mail.php` (`mailers.resend`) e `config/services.php` (`resend.key => env('RESEND_API_KEY')`) permanecem intactos e suficientes; não adicionar Postmark/SES/Mailgun; sem `failover` por padrão.
      Cobre: RF-26, RF-28, CT-04, RNF-07, RNF-09
      Acceptance criteria: `composer.json` `require` lista `resend/resend-php` com constraint `^1.x`; `composer.lock` atualizado; `config('mail.default')` é `array` nos testes; `config('mail.mailers.resend.transport') === 'resend'`; `config('services.resend.key')` é null nos testes; nenhum outro pacote adicionado; `NoCommittedSecretsTest`, `NoNextJsDependencyTest`, `NoSupabaseDependencyTest` verdes.
      Testes: `tests/Feature/Compliance/MailTransportTest.php` — require presente, transporte de teste `array`, mailer resend configurado, chave ausente
- [ ] T14 — PT-BR mail templates and sender identity
      Arquivos: `resources/views/mail/auth/first-access-invite.blade.php`, `resources/views/mail/auth/reset-password.blade.php` (novos, markdown), `app/Notifications/FirstAccessInvite.php`, `app/Notifications/ResetPasswordPtBr.php` (passar a `->markdown(...)`), `lang/pt_BR.json` (novo — strings do tema de e-mail; alternativa FLEXIBLE: publicar `laravel-mail` e traduzir), `tests/Feature/Notifications/AuthNotificationsTest.php` (estender)
      Mudança: mensagens integralmente em PT-BR (assunto, saudação com nome, propósito, botão, validade "72 horas"/"60 minutos", linha de URL alternativa, "Se você não solicitou…"); remetente exclusivamente `config('mail.from')` (`MAIL_FROM_ADDRESS`/`MAIL_FROM_NAME`), sem nome/endereço hardcoded; sem texto ou logo MC Inteligência; sem senha. Confirmar que `MAIL_MAILER=log` grava o corpo com o link no log local.
      Cobre: RF-27, CT-07, UI-16, RNF-12
      Acceptance criteria: `toMail()->render()` das duas notificações contém as strings PT-BR, host do link = host de `APP_URL`, nenhuma ocorrência de valor de senha, nenhuma ocorrência de "MC Inteligência"; enviando com `MAIL_MAILER=array` a mensagem é endereçada só ao usuário e o remetente é `config('mail.from.name')`; `grep -rn "MC Inteligência" resources/views/mail` → 0.
      Testes: `AuthNotificationsTest` estendido — render PT-BR, remetente, destinatário único, sem senha, sem MC
- [ ] T15 — README, `.env.example`, IH-01 and operations documentation
      Arquivos: `README.md`, `.env.example`, `tests/Feature/Compliance/EnvExampleTest.php` (novo)
      Mudança: `.env.example` linha 23 → `APP_NAME="Albuquerque Engenharia"`; bloco mail mantém `MAIL_MAILER=log` e acrescenta chaves de produção comentadas (`# MAIL_MAILER=resend`, `# RESEND_API_KEY=` vazio, `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME="${APP_NAME}"`) e `GESTAO_BOOTSTRAP_PASSWORD` só como nome comentado. `README.md`: tabela de variáveis de produção com `APP_NAME`, `MAIL_MAILER=resend`, `RESEND_API_KEY=<placeholder>`, `MAIL_FROM_ADDRESS=<placeholder>`, `MAIL_FROM_NAME`, `BCRYPT_ROUNDS=12`; SMTP como fallback; seção "E-mail transacional" (log local / array testes / resend produção; passos humanos IH-01; comportamento enquanto não configurado); seção "Bootstrap do primeiro Gestão" (`users:create-gestao`, senha nunca armazenada, troca via "Esqueci minha senha"); validade convite 72 h / reset 60 min e tabela de tokens compartilhada; remover a afirmação das linhas 382-383 de que envio de e-mail está fora de escopo; resumo do runbook da Etapa 10 e checklist diferido da Etapa 11. Nunca uma chave ou senha real.
      Cobre: RF-28, RF-32, RF-33, CT-03, CT-04, UI-15, RNF-07, RNF-12
      Acceptance criteria: `.env.example` contém `APP_NAME="Albuquerque Engenharia"` e `MAIL_MAILER=log` e nenhum valor não-vazio em `RESEND_API_KEY=`; `README.md` contém as 5 variáveis de e-mail/nome com placeholders, a seção IH-01, a documentação do comando `users:create-gestao`, a menção "72 horas" e o runbook/checklist; não existe mais o texto de "envio de e-mail" fora de escopo; `NoCommittedSecretsTest` verde.
      Testes: `tests/Feature/Compliance/EnvExampleTest.php` (APP_NAME, MAIL_MAILER=log, RESEND_API_KEY vazio); `NoCommittedSecretsTest`

## Phase 6: Etapa 5 — Design system

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T16 — Design tokens and token-based component layer
      Arquivos: `resources/css/app.css`, `tests/Feature/Design/ThemeTokensTest.php` (novo)
      Mudança: estender `@theme` (mantendo `--font-sans`) com `--color-primary: #9E0128`, `--color-primary-hover: #802036`, `--color-primary-active: #661F35`, `--color-secondary: #520C1F`, `--color-background: #F7F7F8`, `--color-surface: #FFFFFF`, `--color-border: #E5E7EB`, `--color-text: #202124`, `--color-text-muted: #6B7280`, `--color-focus: #9E0128`, mais semânticos `--color-success`, `--color-warning`, `--color-error`, `--color-info` (azul não-sky), `--color-atraso`, `--color-concluido` e tints `-soft`; reescrever `@layer components` sobre tokens: `.card` (surface/border, `rounded-lg`, `shadow-sm`), `.page-title`/`.section-title`/`.form-label`, `.form-control` (focus ring `focus`, disabled visível, `rounded-md`), `.form-error` (error), `.btn-primary` (primary → hover/active vinho, sem `rounded-full`), `.btn-secondary` (surface, borda neutra, texto vinho), `.btn-danger`, `.badge` + `.badge-neutral/-info/-success/-warning/-error/-secondary/-atraso/-concluido`, `.data-table` (thead background neutro), `.pedido-atrasado` (nome preservado), `.nav-link`/`.nav-link-active` (primary + indicador inferior 2px), `.alert-success/-error/-info`, `.empty-state`; sem gradientes, `shadow-xl` ou `animate-*`. `npm run build` exit 0.
      Cobre: UI-01, UI-02, UI-04, UI-05, UI-06, UI-09, UI-10, UI-11, UI-13, RNF-17
      Acceptance criteria: `@theme` em `resources/css/app.css` declara os 10 tokens nomeados + os 6 semânticos e contém os 9 hex de §16 ligados aos tokens corretos; `primary-hover`/`primary-active` são vinhos (nunca azul); o arquivo não contém `sky-` nem `bg-gradient`; `.btn-primary` referencia `primary`; `.pedido-atrasado` continua existindo; `npm run build` termina com exit 0; suíte completa verde.
      Testes: `tests/Feature/Design/ThemeTokensTest.php` — leitura do CSS: tokens, hex, ausência de `sky-`/gradiente, regras `.btn-primary` e `.pedido-atrasado`; suíte existente verde

## Phase 7: Etapa 6 — Aplicação global da identidade

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T17 — Topbar and app layout on tokens
      Arquivos: `resources/views/layouts/app.blade.php`, `tests/Feature/Livewire/LayoutIdentityTest.php` (novo)
      Mudança: `html` com `bg-background`, body `text-text`; `<header class="bg-surface border-b border-border">` (sem `bg-slate-900`, sombra ≤ `shadow-sm`); marca `{{ config('app.name') }}` em `text-text` (sem nome hardcoded); nav com `.nav-link`/`.nav-link-active` mantendo `aria-current`; nome do usuário `text-text-muted`; badge de papel `.badge .badge-neutral`; "Sair" `.btn-secondary`; manter o `match` de `$navItems` (com 4 itens gestao), sem `<aside>`, sem assinatura MC; manter `@vite`, `@livewireStyles`, `@livewireScripts`.
      Cobre: UI-03, UI-04, UI-07, UI-08, UI-14, UI-15, UI-16, UI-25
      Acceptance criteria: `resources/views/layouts/app.blade.php` não contém `bg-slate-900`, `bg-slate-800`, `sky-`, `<aside` nem "MC Inteligência"; gestao renderiza exatamente 4 links de nav (Dashboard, Kanban, Todos os Pedidos, Usuários); obra/suprimentos sem `Usuários`; o texto da marca é `config('app.name')`.
      Testes: `tests/Feature/Livewire/LayoutIdentityTest.php` — 4 links gestao, ausência de Usuários para outros papéis, ausência de classes escuras/sky/aside/MC nas páginas autenticadas
- [ ] T18 — Auth layout and login redesign (text-only signature; logos in Etapa 9)
      Arquivos: `resources/views/auth/login.blade.php`, `resources/views/livewire/auth/login-form.blade.php`, `phpunit.xml` (`<env name="APP_NAME" value="Albuquerque Engenharia"/>`), `tests/Feature/Auth/LoginScreenIdentityTest.php` (novo)
      Mudança: layout auth em `bg-background`, coluna centralizada, slot reservado para a logo acima do `h1` (vazio até T28 — nenhuma referência a imagem agora), `h1` `{{ config('app.name') }}`, card `.card` (`rounded-lg`, `shadow-sm`), rodapé `<p class="... text-xs text-text-muted">Tecnologia por MC Inteligência</p>` (a `<img>` MC entra em T28) — rodapé só no layout auth, herdado por esqueci-senha/reset/convite, nunca em `layouts/app`. Login: `h2` "Entrar no sistema", inputs `.form-control` + `.form-label`, erros `.form-error role="alert"`, link de T12 em `text-primary`, botão `.btn-primary` largura total, sem `sky-*`. `<title>` mantém `config('app.name')`.
      Cobre: UI-09, UI-10, UI-14, UI-15, UI-16, UI-24, UI-25
      Acceptance criteria: `GET /login` contém "Albuquerque Engenharia", "Entrar no sistema", "Esqueci minha senha", "Entrar" e "Tecnologia por MC Inteligência"; o botão de submit tem `btn-primary` e nenhuma classe `sky-`; `grep -rn "Albuquerque Engenharia" resources/views app` → 0; `phpunit.xml` define `APP_NAME`; as páginas `password.request`, `password.reset` e `invite.show` mostram o mesmo `h1` e a mesma assinatura; `LoginFormTest`/`LoginTest` inalterados e verdes; nenhum arquivo em `resources/views` referencia `logo-albuquerque` ou `logo-mc`.
      Testes: `tests/Feature/Auth/LoginScreenIdentityTest.php` — textos literais, classe do botão, ausência do literal da marca nas views/app, herança nas 3 páginas auth
- [ ] T19 — Identity on Obra, Suprimentos and Kanban views
      Arquivos: `resources/views/livewire/obra/acompanhamento.blade.php`, `resources/views/livewire/obra/nova-solicitacao.blade.php`, `resources/views/livewire/obra/pedido-detalhe.blade.php`, `resources/views/livewire/suprimentos/todos-pedidos.blade.php`, `resources/views/livewire/suprimentos/pedido-detalhe.blade.php`, `resources/views/livewire/kanban/kanban-board.blade.php`, `resources/views/livewire/kanban/pedido-card.blade.php`
      Mudança: substituir classes de paleta hardcoded (`sky-*`, superfícies `slate-800/900`, `violet-*`) por tokens/utilitários: fundo neutro, `.card`, filtros/formulários `.form-control`/`.form-label`, botões `.btn-*`, tabelas `.data-table`, colunas Kanban `bg-background` com cabeçalho `border-border` e cards `bg-surface`, indicadores ativos `text-primary`/`border-primary`, estados vazios `.empty-state`, flashes `.alert-*`, paginação neutra, estados hover/focus/active/disabled via tokens. Não alterar `wire:*`, `data-*`, `id`, `aria-*`, textos de label ou ordem de controles. Manter `.pedido-atrasado`.
      Cobre: UI-03, UI-04, UI-05, UI-06, UI-13, UI-14
      Acceptance criteria: `grep -rE "(bg|text|border|ring|from|to)-sky-[0-9]+" resources/views/livewire/obra resources/views/livewire/suprimentos resources/views/livewire/kanban` → 0; nenhuma `bg-slate-800/900`, `bg-gradient`, `shadow-xl` nesses arquivos; `.pedido-atrasado` continua aplicado a cards atrasados; todos os testes existentes dessas telas verdes sem alteração.
      Testes: `AcompanhamentoTest`, `NovaSolicitacaoTest`, `PedidoDetalheObraTest`, `PedidoDetalheSuprimentosTest`, `TodosPedidosFiltersTest`, `KanbanBoardTest`, `PedidoCardRenderTest`, `AccessibleStatusControlTest`, `CancelPedidoControlTest`, `QueryCountTest` — verdes inalterados
- [ ] T20 — Identity on Gestão views, users area and auth pages
      Arquivos: `resources/views/livewire/gestao/dashboard.blade.php`, `resources/views/livewire/gestao/kanban-read-only.blade.php`, `resources/views/livewire/gestao/pedido-card-read-only.blade.php`, `resources/views/livewire/gestao/pedido-detalhe.blade.php`, `resources/views/livewire/gestao/todos-pedidos.blade.php`, `resources/views/livewire/gestao/usuarios/index.blade.php`, `resources/views/livewire/gestao/usuarios/form.blade.php`, `resources/views/livewire/auth/forgot-password.blade.php`, `resources/views/livewire/auth/reset-password.blade.php`, `resources/views/livewire/auth/accept-invite.blade.php`
      Mudança: dashboard — barras `porStatus`/`porObra` em `bg-primary` sobre trilho `bg-background`; buckets `prazos` com 3 cores semânticas distintas (`success`, `warning`, `atraso`); cards de indicador `.card` com acentos semânticos, mantendo `data-testid`/`data-value`; demais telas gestao como T19; área de usuários com `.data-table`, `.card`, `.btn-primary/-secondary`, badge `.badge-success`/`.badge-neutral`, formulário `.form-control`, `.alert-*`; páginas auth com `.form-control`, `.btn-primary`, confirmação `.alert-info`, links `text-primary`. Sem `sky-*`, superfícies escuras, gradientes ou `shadow-xl`.
      Cobre: UI-04, UI-11, UI-12, UI-13, UI-14, UI-23, UI-24
      Acceptance criteria: `grep -rE "sky-[0-9]+" resources/views/livewire/gestao resources/views/livewire/auth` → 0; `dashboard.blade.php` não contém `bg-sky-500` nem `bg-violet-500` e mantém 3 classes distintas para os buckets de prazo e os atributos `data-testid`/`data-value`; as views de usuários e auth usam apenas as classes de componente/tokens; testes dessas telas verdes inalterados.
      Testes: `DashboardIndicatorsTest`, `DashboardFiltersTest`, `DashboardDrillDownTest`, `GestaoKanbanReadOnlyTest`, `PedidoDetalheGestaoTest`, `UsuariosIndexTest`, `UsuariosFormTest`, `PasswordRecoveryRequestTest`, `PasswordResetTest`, `FirstAccessInviteTest` — verdes inalterados
- [ ] T21 — Semantic badges and shared components on tokens
      Arquivos: `resources/views/components/status-badge.blade.php`, `resources/views/components/priority-badge.blade.php`, `resources/views/components/pedido-table.blade.php`, `resources/views/components/pedido-summary.blade.php`, `resources/views/components/pedido-history-timeline.blade.php`, `resources/views/components/atraso-indicator.blade.php`, `tests/Feature/Livewire/SemanticBadgeTest.php` (novo)
      Mudança: `status-badge`: solicitado → `badge-neutral`, em_analise → `badge-info`, em_compra_preparacao → `badge-secondary` (tint vinho claro), aguardando_entrega → `badge-warning`, entregue → `badge-concluido`, cancelado → `badge-error`; `priority-badge`: baixa neutral, normal info, alta warning, urgente error sólido — cada slug em classe distinta, vermelho institucional não aplicado a todos os estados; manter `data-status`/`data-priority` e `$attributes->merge`; `atraso-indicator` → `text-atraso`; `pedido-table` → tokens de `.data-table`; `pedido-summary`/`pedido-history-timeline` → `text-text`/`text-text-muted`/`border-border`, ponto da timeline `bg-primary` só no evento mais recente.
      Cobre: UI-05, UI-11, UI-14
      Acceptance criteria: renderizar `x-status-badge` para os 6 status produz 6 conjuntos de classes distintos mantendo `data-status`; idem para as 4 prioridades com `data-priority`; nenhuma classe `sky-` nos 6 componentes; `PedidoCardRenderTest`, `AccessibleStatusControlTest`, `PedidoDetalhe*Test` verdes inalterados.
      Testes: `tests/Feature/Livewire/SemanticBadgeTest.php` — distinção por slug e atributos `data-*`; testes existentes de componentes verdes
- [ ] T22 — Identity compliance gate (grep-based tests)
      Arquivos: `tests/Feature/Compliance/BrandIdentityComplianceTest.php` (novo)
      Mudança: teste baseado em `Finder` (estilo `NoCommittedSecretsTest`): (a) 0 ocorrências de `(bg|text|border|ring|from|to)-sky-[0-9]+` em `resources/views` e `resources/css`; (b) nenhum `bg-slate-800`/`bg-slate-900` em `resources/views/layouts` e `resources/views/auth`; (c) nenhum `bg-gradient-`, `shadow-xl`, `shadow-2xl`, `animate-` (exceto relacionados a `wire:loading`) em `resources/views`; (d) "MC Inteligência" só em `resources/views/auth/login.blade.php`; (e) literal "Albuquerque Engenharia" ausente de `resources/views` e `app`; (f) nenhum `rounded-full` nas regras `.btn-*` e nenhum `<aside` em layouts; (g) nenhuma saída de `->password` em `resources/views`.
      Cobre: UI-01, UI-04, UI-07, UI-09, UI-13, UI-15, UI-16, UI-25, RF-25
      Acceptance criteria: `tests/Feature/Compliance/BrandIdentityComplianceTest.php` existe com as 7 verificações e passa contra o código da fase; `grep -rE "(bg|text|border|ring|from|to)-sky-[0-9]+" resources/views resources/css | wc -l` = 0.
      Testes: a própria tarefa é o teste — verde após T17–T21

## Phase 8: Etapa 7 — Responsividade e refinamento

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T23 — Responsiveness and accessibility verification (3 viewports)
      Arquivos: `tests/Browser/ResponsiveIdentityTest.php` (novo); correções pontuais de views apenas se houver overflow/foco (mesmos arquivos de T17–T21)
      Mudança: teste Pest Browser visitando `/login`, `/suprimentos/kanban`, `/gestao/dashboard`, `/gestao/usuarios`, `/gestao/usuarios/novo`, `/esqueci-senha` em viewports ≥1280 px, 768–1024 px e ≤414 px (confirmar API de resize do pest-plugin-browser via search-docs); asserções: `document.documentElement.scrollWidth <= clientWidth` (sem overflow horizontal), controle primário visível, `assertNoJavascriptErrors()`, todo `<input>` com `<label for>` correspondente; verificar foco visível (`focus:ring-2`) em botões/links/inputs; registrar contraste WCAG (`#FFFFFF` sobre `#9E0128` = 8.4:1; `#202124` sobre `#FFFFFF` = 16.1:1; `#6B7280` sobre `#FFFFFF` = 4.8:1) para T24; corrigir overflows (ex.: `overflow-x-auto` na tabela de usuários) sem alterar comportamento.
      Cobre: UI-21, UI-22
      Acceptance criteria: `tests/Browser/ResponsiveIdentityTest.php` existe, cobre as 6 telas nos 3 viewports e passa; nenhuma tela apresenta overflow horizontal do documento; todos os inputs têm label associado; `AccessibleStatusControlTest` e `tests/Browser/DemoRoteiroTest.php` verdes.
      Testes: `tests/Browser/ResponsiveIdentityTest.php`; `AccessibleStatusControlTest`; `DemoRoteiroTest`
- [ ] T24 — Visual review artifact (§46 quality gate)
      Arquivos: `.spec/features/ajustes-finais-albuquerque/visual-review.md` (novo artefato na pasta da feature — não em `docs/`)
      Mudança: matriz de revisão por tela (login, esqueci-senha, redefinir-senha, primeiro-acesso, topbar, Obra acompanhamento, nova solicitação, Obra detalhe, Suprimentos todos os pedidos, Suprimentos detalhe, Kanban, Gestão dashboard, Gestão kanban, Gestão todos os pedidos, Gestão detalhe, Usuários index, Usuários form) × 12 itens de §46 (cores, spacing, tipografia, alinhamento, bordas, radius, sombras, estados, responsividade, logos, hierarquia visual, legibilidade) × 11 anti-padrões de §26 (pass/fail por tela) × 26 itens de §27 (UI-14); linha obrigatória "§19 Sidebar: N/A — navegação topbar-only" e itens de sidebar de §27/§34/§51 marcados N/A com a mesma nota; proporção light de §15 por tela (nenhum container de largura total em vermelho/vinho); contrastes de T23; coluna "logos" marcada "pendente Etapa 9" até T28. Trocar classes Tailwind não conta como concluído (RNF-16).
      Cobre: UI-03, UI-07, UI-13, UI-14, RNF-16
      Acceptance criteria: `visual-review.md` existe com as 17 telas, os 12 itens de §46, as 11 linhas de §26 por tela, a matriz de 26 itens de §27, a linha literal "§19 Sidebar: N/A — navegação topbar-only", os valores de contraste e a coluna de logos marcada como pendente.
      Testes: sem teste automatizado — artefato revisado pelo desenvolvedor; T28 acrescenta as linhas de logos

## Phase 9: Etapa 8 — Testes e regressão

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T25 — Full regression and preservation gates (Etapa 8)
      Arquivos: nenhum código de aplicação (correções apenas no arquivo que regrediu, se houver); anotação de contagens finais em `.spec/features/ajustes-finais-albuquerque/audit-etapa1.md`
      Mudança: executar em ordem `vendor/bin/pint --dirty --format agent`; `php artisan test --compact` (suíte inteira: baseline de 296 testes com asserções inalteradas + novos, 0 falhas); `npm run build` (exit 0); `php artisan test --compact tests/Browser/DemoRoteiroTest.php`; diff de preservação `git diff --name-only 82e4d48 -- app/Actions/Pedidos app/Domain app/Policies/PedidoPolicy.php app/Policies/PedidoEventPolicy.php app/Enums app/Livewire/Auth/LoginForm.php app/Models/PedidoEvent.php database/migrations` vazio; `git diff 82e4d48 -- tests/` só com adições (nenhuma asserção existente editada); revalidar a lista de §44 mapeando cada fluxo aos testes verdes (login, Obra, criação de pedido, acompanhamento, Suprimentos, Kanban, status, responsável, prioridade, previsão, histórico, Gestão, dashboard, autorização, isolamento entre obras); registrar contagens finais.
      Cobre: RF-01, TC-17, RNF-17
      Acceptance criteria: suíte completa com 0 falhas; o comando de diff de preservação retorna vazio; `git diff 82e4d48 --stat -- tests/` mostra apenas arquivos novos ou hunks de adição; `npm run build` exit 0; `DemoRoteiroTest` verde; contagens finais registradas no artefato de auditoria.
      Testes: suíte completa (`php artisan test --compact`), `tests/Browser/DemoRoteiroTest.php`
- [ ] T26 — E2E for the new flows (browser)
      Arquivos: `tests/Browser/AuthRecoveryAndUsersTest.php` (novo)
      Mudança: cenários Pest Browser com `MAIL_MAILER=array`: (1) login como `gestao.demo@example.com` → `Usuários` → criar usuário Obra com 1 obra → linha com badge `Ativo` e nome da obra → `Desativar` → `Inativo` → `Ativar` → `Reenviar convite` → confirmação; (2) `/login` → "Esqueci minha senha" → enviar `obra.demo@example.com` → texto genérico; e-mail desconhecido → mesmo texto; (3) aceitar convite ponta a ponta lendo o token das mensagens do transporte `array` → `invite.show` → definir senha → login cai em `/obra/pedidos`. Seguir os helpers de `DemoRoteiroTest` (aguardar `wire:model` antes de digitar; fazer logout entre atores).
      Cobre: UI-21, TC-01, TC-07, TC-10, TC-16
      Acceptance criteria: `tests/Browser/AuthRecoveryAndUsersTest.php` existe com os 3 cenários e passa junto com `DemoRoteiroTest`; nenhum teste existente foi alterado.
      Testes: a própria tarefa é o teste — `php artisan test --compact tests/Browser`

## Phase 10: Etapa 9 — LOGOS (última etapa de implementação)

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T27 — Etapa 9: copy and version the official logo assets
      Arquivos: `public/images/logo-albuquerque.png`, `public/images/logo-mc.png` (novos, binários), `tests/Feature/Compliance/BrandAssetsTest.php` (novo)
      Mudança: antes de copiar, confirmar existência e nomes dos originais em `/mnt/c/Users/leool/OneDrive/Documentos/Projetos/MC-Inteligência_Albuquerque - Sistema de Solicitações e Compras/{logo_Albuquerque.png,logo_MC.png}`, inspecionar com `file`/`getimagesize` (1063×345 RGBA 209281 B; 1305×200 RGBA 23177 B) e registrar `sha256sum`; copiar com `cp` (nunca `mv`, nunca editar/redimensionar/reencodar) para `public/images/`; verificar que os `sha256sum` das cópias são iguais aos originais e que tamanho/mtime dos originais não mudaram; `git add public/images/*.png` (conferir `.gitignore`); não referenciar os arquivos em nenhuma view ainda.
      Cobre: UI-17, UI-18, UI-19, UI-20, CT-06, IH-02
      Acceptance criteria: os dois arquivos existem em `public/images/`, `getimagesize()` retorna 1063×345 e 1305×200 `image/png`, tamanhos 209281 e 23177 bytes, ambos listados em `git ls-files public/images`; nenhuma string `/mnt/c/` ou `C:\Users` em `app/`, `resources/`, `public/`, `config/`; os originais permanecem inalterados; nenhuma view referencia as imagens nesta tarefa.
      Testes: `tests/Feature/Compliance/BrandAssetsTest.php` — existência, dimensões, tamanho, rastreamento no Git, ausência de caminhos Windows
- [ ] T28 — Etapa 9: apply the Albuquerque logo and the MC signature logo
      Arquivos: `resources/views/auth/login.blade.php`, `tests/Feature/Auth/LoginScreenIdentityTest.php` (estender), `.spec/features/ajustes-finais-albuquerque/visual-review.md` (linhas de logos)
      Mudança: no slot do layout auth: `<img src="{{ asset('images/logo-albuquerque.png') }}" alt="{{ config('app.name') }}" class="mx-auto mb-4 h-14 w-auto rounded-md sm:h-[72px]" width="1063" height="345">` (proporção preservada por `w-auto` + atributos intrínsecos; cantos arredondados; respiro; 56 px mobile / 72 px desktop, ≤ 60 % da largura do card); no rodapé: `<img src="{{ asset('images/logo-mc.png') }}" alt="MC Inteligência" class="h-4 w-auto" width="1305" height="200">` alinhada ao texto "Tecnologia por MC Inteligência" em `text-text-muted` (menor que a logo Albuquerque e < 2× a altura da linha); revisar proporções, alinhamentos e responsividade nos 3 viewports (`ResponsiveIdentityTest`); `npm run build` exit 0; atualizar a coluna "logos" de `visual-review.md` para as 4 telas auth; nenhuma logo em `layouts/app.blade.php`, sem favicon/título MC; verificar com `git log --oneline -- public/images resources/views/auth/login.blade.php` que estes são os últimos commits de implementação.
      Cobre: UI-15, UI-16, UI-17, UI-18, UI-20, UI-25, CT-06
      Acceptance criteria: `/login`, `password.request`, `password.reset` e `invite.show` contêm `images/logo-albuquerque.png` com `w-auto` (sem par fixo `w-*`+`h-*` conflitante) e a string "Tecnologia por MC Inteligência" no mesmo elemento pai de `images/logo-mc.png` com altura menor que a da logo Albuquerque; `/gestao/dashboard` não contém nenhuma das imagens; `BrandIdentityComplianceTest` e `ResponsiveIdentityTest` verdes; `npm run build` exit 0; `visual-review.md` com a coluna de logos preenchida.
      Testes: `LoginScreenIdentityTest` estendido (imagens nas 4 telas auth, adjacência da assinatura, ausência nas páginas autenticadas); `BrandIdentityComplianceTest`; `ResponsiveIdentityTest`

## Phase 11: Etapa 10 — Preparação para deploy (gates automatizáveis)

Antes de implementar, leia:
1. `.spec/features/ajustes-finais-albuquerque/SPEC.md` — requisitos RIGID que esta fase cobre
2. `.spec/features/ajustes-finais-albuquerque/PLAN.md` — decomposição completa, dependências e riscos (seção "Operator runbook — Etapa 10": push, variáveis Railway, `users:create-gestao` em produção, verificação de login, desativação das contas demo, IH-01 — passos humanos, NÃO executar nesta fase)

- [ ] T29 — Etapa 10: pre-deploy gates (automatable part)
      Arquivos: nenhum (verificação); `README.md` apenas se um detalhe do runbook se mostrou incorreto em T25–T28
      Mudança: (1) `git status` limpo e `git diff --stat 82e4d48..HEAD` revisado — todo caminho alterado pertence às tarefas T02–T28 (sem `.env`, sem `public/build`); (2) segredos: `php artisan test --compact --filter=NoCommittedSecrets` verde e `git grep -nE "RESEND_API_KEY\s*=\s*re_" -- . ':!README.md'` vazio; nenhum literal de senha em `app/Console/Commands/CreateGestaoUser.php`; (3) `vendor/bin/pint --dirty`, suíte completa, `npm run build` exit 0, suíte Browser verde; (4) `composer.lock` inclui `resend/resend-php` e `composer install --no-dev --dry-run` resolve; (5) `ls database/migrations | wc -l` = 14 (pre-deploy `migrate --force` será no-op; nunca `migrate:fresh`); (6) commits descritivos (`feat(phase-N): …`) e o commit de logos (phase-10) é o último de implementação; (7) `README.md` com tabela de variáveis, IH-01 e runbook; `.env.example` com `APP_NAME="Albuquerque Engenharia"` e `MAIL_MAILER=log`. Push, deploy Railway e passos de produção ficam para o operador (runbook em PLAN.md).
      Cobre: RNF-07, RNF-13, RNF-14, RNF-15, RNF-17
      Acceptance criteria: todos os 7 gates acima verdes e registrados no artefato de auditoria; nenhum arquivo `.env` ou segredo versionado; nenhuma migration nova; `composer install --no-dev --dry-run` sem erro; suíte completa e build verdes; nenhuma alteração em Railway, produção ou banco feita pelo agente.
      Testes: suíte completa + `NoCommittedSecretsTest`, `MailTransportTest`, `EnvExampleTest`, `BrandAssetsTest`, `BrandIdentityComplianceTest`
