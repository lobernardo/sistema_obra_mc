# Phases: security-hardening-production

Gerado por /plan a partir de PLAN.md — view executável para `./ralph.sh .spec/features/security-hardening-production/PHASES.md`.

Ordem RIGID (description §17): Phase 1 escopo `visibleTo` → Phase 2 obra inativa → Phase 3 rate limiting → Phase 4 invalidação de sessão → Phase 5 auditoria administrativa → Phase 6 eventos de autenticação → Phase 7 suíte adversarial → Phase 8 revisão de configuração → Phase 9 gates completos + relatório. Regras transversais em toda fase: nunca editar migrations existentes (só migrations novas, forward-only); nunca remover/enfraquecer teste existente; nunca Global Scope em `Pedido`; nunca RLS; nunca alterar Railway/produção nem a branch `build/v0-demo-laravel`; nunca `composer update`/`npm update`/`npm audit fix`; código compatível com PHP 8.4; nunca ler `.env`; nenhum segredo em trilha, chave de limiter, log ou arquivo versionado; `php artisan make:*` para arquivos novos; `vendor/bin/pint --dirty --format agent` e testes focados verdes antes do checkpoint `feat(phase-N): …` na branch `feat/security-hardening-production`. Banco de testes PostgreSQL em `127.0.0.1:5434` (`docker start sistema-obra-pgsql-testing`). Condições de parada (description §21): regra de negócio fora de D-01..D-12, divergência da matriz §3, migration destrutiva, reescrita de dados, mudança em produção, RLS, Global Scope, incompatibilidade PHP 8.4, teste removido/enfraquecido, exposição de segredo, mudança substancial de arquitetura → PARAR e reportar, nunca improvisar.

## Phase 1: Fase A — Escopo centralizado `Pedido::visibleTo`

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-01..RF-04, CT-01, RNF-03, RNF-07)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T01 — `Pedido::visibleTo(User)` local scope
      Arquivos: `app/Models/Pedido.php`; `tests/Feature/Authorization/PedidoVisibleToScopeTest.php` (novo)
      Mudança: adicionar `#[Scope] protected function visibleTo(Builder $query, User $user): Builder` (convenção `app/Models/Status.php:35`; invocado como `Pedido::query()->visibleTo($user)` — CT-01). Resolver por `RoleSlug::tryFrom((string) $user->role?->slug)`: `Obra` → `whereIn('obra_id', $user->obras()->select('obras.id'))` (subquery, RNF-07); `Suprimentos`/`Gestao` → query inalterada; `null`/desconhecido → `whereRaw('1 = 0')`. Sem exceção, sem efeito colateral, sem filtro por `obras.is_active` (D-06). Sem `addGlobalScope`/`ScopedBy`. Docblock cita RF-01, CT-01, D-06.
      Cobre: RF-01, CT-01, RNF-03, RNF-07
      Acceptance criteria: `grep -n 'function visibleTo' app/Models/Pedido.php` = 1 e `grep -cE 'addGlobalScope|ScopedBy' app/Models/Pedido.php` = 0; o teste novo prova que `Pedido::query()->visibleTo($user)->pluck('id')` retorna exatamente os ids da obra associada para `obra` (incluindo obra com `is_active=false`), todos para `suprimentos` e `gestao`, e zero para papel desconhecido; composição com `with()/latest()/paginate()` mantém o mesmo conjunto.
      Testes: `tests/Feature/Authorization/PedidoVisibleToScopeTest.php` — 3 papéis + papel desconhecido + obra inativa ainda visível + composabilidade; `php artisan test --compact tests/Feature/Authorization/PedidoVisibleToScopeTest.php tests/Unit/Models/PedidoModelTest.php`
- [ ] T02 — Obra area reads through `visibleTo` (listing + detail existence check)
      Arquivos: `app/Livewire/Obra/Acompanhamento.php`, `app/Livewire/Obra/PedidoDetalhe.php`; `tests/Feature/Livewire/PedidoDetalheObraTest.php`, `tests/Feature/Livewire/AcompanhamentoTest.php` (apenas acrescentar)
      Mudança: `Acompanhamento::pedidos()` → `Pedido::query()->visibleTo(Auth::user())->with([...])->latest('requested_at')->paginate(10)`, removendo o `pluck` e o `whereIn('obra_id', …)` (RF-02). `PedidoDetalhe::mount(Pedido $pedido)` mantém route-model binding (404 se o id não existe) e, antes de `$this->authorize('view', $pedido)`, aplica o escopo como existence check lançando `AuthorizationException`: `throw_unless(Pedido::query()->visibleTo(Auth::user())->whereKey($pedido->getKey())->exists(), AuthorizationException::class, 'Pedido fora do escopo do solicitante.')` — nunca `findOrFail`/`firstOrFail` pelo escopo (RF-03). `PedidoPolicy::view` intocada. Docblocks citam RF-02/RF-03.
      Cobre: RF-02, RF-03, CT-06, RNF-07
      Acceptance criteria: `grep -c "whereIn('obra_id'" app/Livewire/Obra/Acompanhamento.php` = 0 e `grep -c 'visibleTo(' app/Livewire/Obra/Acompanhamento.php app/Livewire/Obra/PedidoDetalhe.php` ≥ 1 em cada; `grep -cE 'findOrFail|firstOrFail' app/Livewire/Obra/PedidoDetalhe.php` = 0; usuário obra A em `/obra/pedidos/{pedidoB}` → 403; `Livewire::test(PedidoDetalhe::class, ['pedido' => $pedidoB])` → `AuthorizationException`; id inexistente → 404; próprio pedido → 200; `AcompanhamentoTest`, `PedidoDetalheObraTest:54` e `QueryCountTest` T32 verdes com thresholds inalterados.
      Testes: acrescentar em `tests/Feature/Livewire/PedidoDetalheObraTest.php` — cross-obra 403, mount forjado → `AuthorizationException`, inexistente 404; `php artisan test --compact tests/Feature/Livewire/AcompanhamentoTest.php tests/Feature/Livewire/PedidoDetalheObraTest.php tests/Feature/Livewire/ObraScreensRouteTest.php tests/Feature/Performance/QueryCountTest.php tests/Feature/Authorization`
- [ ] T03 — Mechanical regression guard for Obra-area queries
      Arquivos: `tests/Feature/Compliance/ObraVisibleToGuardTest.php` (novo)
      Mudança: teste Pest que faz glob de `app/Livewire/Obra/*.php`, tokeniza com `PhpToken::tokenize` e afirma: (a) toda referência estática `Pedido::` é seguida de `visibleTo` antes do próximo `;` da mesma instrução; (b) zero `whereIn('obra_id'` e zero `Pedido::find`/`findOrFail`/`firstOrFail` nesses arquivos; (c) `NovaSolicitacao.php` não tem chamada estática `Pedido::`; (d) `app/Models/Pedido.php` sem `addGlobalScope`/`ScopedBy` (RNF-03). Docblock nomeia o mecanismo, a limitação documentada (só pontos de entrada estáticos `Pedido::`; leituras por relação são pedido-scoped após o mount check e cobertas por G-01..G-03), referencia `Acompanhamento::pedidos()` e `PedidoDetalhe::mount()` por classe::método, e registra que `PedidoPolicyTest` fixa a segunda barreira (nota de mutação RF-03).
      Cobre: RF-04, RNF-03
      Acceptance criteria: arquivo existe em `tests/Feature/Compliance/` com docblock contendo `PhpToken`, `Acompanhamento::pedidos()` e `PedidoDetalhe::mount()`; passa na branch; remover manualmente `->visibleTo(` de `Acompanhamento` (sem commitar) faz o teste falhar.
      Testes: `php artisan test --compact tests/Feature/Compliance/ObraVisibleToGuardTest.php`

## Phase 2: Fase B — Obra inativa não recebe nova solicitação

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-05..RF-08, UI-01, CT-05, D-06)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T04 — `Obra::active()` scope and inactive-obra rejection in `CreatePedidoAction`
      Arquivos: `app/Models/Obra.php`, `app/Actions/Pedidos/CreatePedidoAction.php`, `database/factories/ObraFactory.php`; `tests/Feature/Actions/CreatePedidoActionTest.php` (acrescentar)
      Mudança: `Obra`: `#[Scope] protected function active(Builder $query): Builder` → `where('is_active', true)`; `ObraFactory::inactive()` (`is_active => false`). `CreatePedidoAction::execute`: após a checagem de associação existente (mensagem inalterada) e ANTES de `DB::transaction` (sequência `pedido_code_sequence` nunca consumida), `if (! $requester->obras()->active()->whereKey($validated['obra_id'])->exists())` → `ValidationException::withMessages(['obra_id' => 'A obra informada está inativa e não recebe novas solicitações.'])` (CT-05, mesma chave `obra_id`). Docblock cita RF-05/CT-05.
      Cobre: RF-05, CT-05, G-14 (backend)
      Acceptance criteria: `grep -n 'function active' app/Models/Obra.php` = 1; `grep -n 'function inactive' database/factories/ObraFactory.php` = 1; chamada direta da Action com obra inativa associada → `ValidationException` com chave `obra_id` e a mensagem PT-BR; contagens de `pedidos` e `pedido_events` inalteradas; `last_value`/`is_called` de `pedido_code_sequence` inalterados após a tentativa; criação com obra ativa continua funcionando.
      Testes: acrescentar em `tests/Feature/Actions/CreatePedidoActionTest.php` — obra inativa rejeitada sem insert e sem consumir sequência; `php artisan test --compact tests/Feature/Actions/CreatePedidoActionTest.php tests/Feature/Seeders`
- [ ] T05 — `NovaSolicitacao` lists only active obras; UI-01 empty-state
      Arquivos: `app/Livewire/Obra/NovaSolicitacao.php`, `resources/views/livewire/obra/nova-solicitacao.blade.php`; `tests/Feature/Livewire/NovaSolicitacaoTest.php` (acrescentar)
      Mudança: `obras()` → `Auth::user()->obras()->active()->orderBy('name')->get()`. View: quando `$obras->isEmpty()` renderizar `<p role="status">Nenhuma obra ativa está associada ao seu usuário. Fale com a Gestão.</p>` (classe de alerta já existente em `resources/css/app.css`, sem novos tokens — AC-N06) e apenas o link "Voltar"; o `<form>` com o botão de envio só quando `$obras->isNotEmpty()`. `submit()` inalterado (a Action revalida — RF-05). Docblock cita RF-06/UI-01.
      Cobre: RF-06, UI-01, CT-06
      Acceptance criteria: `grep -c 'active()' app/Livewire/Obra/NovaSolicitacao.php` ≥ 1; usuário com 1 obra ativa + 1 inativa vê o nome da ativa e não vê o da inativa; usuário só com obra inativa vê o aviso `Nenhuma obra ativa está associada ao seu usuário. Fale com a Gestão.`, nenhum `<option>` da obra inativa e nenhum botão `Enviar solicitação`; `set('obra_id', $inativa->id)->call('submit')` → `assertHasErrors(['obra_id'])` e `pedidos` inalterado; `BrandIdentityComplianceTest` e `BladeEscapingTest` verdes.
      Testes: acrescentar em `tests/Feature/Livewire/NovaSolicitacaoTest.php` — select só ativas, estado vazio UI-01, id inativo forjado; `php artisan test --compact tests/Feature/Livewire/NovaSolicitacaoTest.php tests/Feature/Livewire/ObraScreensRouteTest.php tests/Feature/Compliance/BrandIdentityComplianceTest.php tests/Feature/Security/BladeEscapingTest.php`
- [ ] T06 — Read-side preservation proofs for inactive obras (D-06, RF-08)
      Arquivos: `tests/Feature/Livewire/ObraInativaPreservaHistoricoTest.php` (novo)
      Mudança: somente teste; docblock cita D-06, RF-07, RF-08. Cenários: obra X com pedido existente é desativada depois; usuário obra de X: GET `/obra/pedidos` → 200 com `assertSee($pedido->code)`; GET `/obra/pedidos/{id}` → 200; `Pedido::visibleTo($user)` ainda retorna o pedido; `DashboardIndicatorsService::compute([])` conta o pedido; `KanbanBoard` (suprimentos) ainda renderiza o card; contagens e `updated_at` de `pedidos`/`pedido_events` inalterados após a desativação.
      Cobre: RF-07, RF-08, AC-N04
      Acceptance criteria: arquivo existe com docblock contendo `D-06`; todos os cenários acima passam; `KanbanBoardTest`, `TodosPedidosFiltersTest`, `DashboardIndicatorsTest`, `GestaoKanbanReadOnlyTest` verdes.
      Testes: `php artisan test --compact tests/Feature/Livewire/ObraInativaPreservaHistoricoTest.php tests/Feature/Livewire/KanbanBoardTest.php tests/Feature/Livewire/TodosPedidosFiltersTest.php tests/Feature/Livewire/DashboardIndicatorsTest.php tests/Feature/Livewire/GestaoKanbanReadOnlyTest.php`

## Phase 3: Fase C — Rate limiting nativo em login e recuperação

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-09..RF-13, UI-02, UI-03, CT-04, D-01, D-02, RNF-09, RNF-11)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T07 — Named limiters in `AppServiceProvider` + `AuthenticationRateLimiter` service
      Arquivos: `app/Providers/AppServiceProvider.php`; `app/Services/AuthenticationRateLimiter.php` (novo via `php artisan make:class Services/AuthenticationRateLimiter --no-interaction`); `tests/Unit/Services/AuthenticationRateLimiterTest.php` (novo)
      Mudança: em `AppServiceProvider::boot()` definir com literais (D-02, única fonte, nunca env/config): `RateLimiter::for('login', fn () => Limit::perMinute(5))`, `RateLimiter::for('login-account', fn () => Limit::perMinutes(15, 20))`, `RateLimiter::for('recovery', fn () => Limit::perMinute(3))`, `RateLimiter::for('recovery-ip', fn () => Limit::perMinute(6))`, docblock citando RF-09/RF-11/D-01/D-02. Serviço final usando só a facade `RateLimiter`: `static normalizeEmail(string): string` (`mb_strtolower(trim())`), `tooManyLoginAttempts(string $email, string $ip): bool`, `hitLogin`, `clearLogin` (ambas as chaves), `tooManyRecoveryAttempts`, `hitRecovery`. Chaves `login:{sha256(email)}:{ip}`, `login-account:{sha256(email)}`, `recovery:{sha256(email)}:{ip}`, `recovery-ip:{ip}` (e-mail hasheado — RNF-09). Limites lidos dos `Limit` resolvidos (`RateLimiter::limiter('login')(null)->maxAttempts`/`->decaySeconds`). Nunca recebe senha. Store = cache padrão `CACHE_STORE` (RF-13; `config/cache.php` sem chave `limiter`).
      Cobre: RF-09, RF-11, RF-12, RF-13, RNF-09
      Acceptance criteria: `grep -c "RateLimiter::for('" app/Providers/AppServiceProvider.php` = 4 com os nomes `login`, `login-account`, `recovery`, `recovery-ip`; `grep -rn 'env(' app/Services/AuthenticationRateLimiter.php` = 0; o teste unitário prova normalização (3 variantes → 1 chave), limites exatos 5/60, 20/900, 3/60, 6/60, `tooManyLoginAttempts` true após 5 hits e false após `clearLogin`, hits visíveis via `RateLimiter::attempts()` e zerados após `Cache::flush()` (cache-backed), nenhuma chave contém o e-mail em claro.
      Testes: `tests/Unit/Services/AuthenticationRateLimiterTest.php`; `php artisan test --compact tests/Unit/Services/AuthenticationRateLimiterTest.php tests/Feature/BootstrapTest.php`
- [ ] T08 — Login limiters in `LoginForm::authenticate` (UI-02, CT-04)
      Arquivos: `app/Livewire/Auth/LoginForm.php`; `tests/Feature/Auth/LoginRateLimitTest.php` (novo); `tests/Feature/Livewire/LoginFormTest.php` (acrescentar)
      Mudança: `authenticate(AuthenticationRateLimiter $limiter)`: (1) `$this->email = AuthenticationRateLimiter::normalizeEmail($this->email)` ANTES de `$this->validate()` (RF-12, independente de `TrimStrings`); (2) `$ip = request()->ip()`; (3) se `tooManyLoginAttempts` → `ValidationException::withMessages(['email' => 'Muitas tentativas. Aguarde alguns instantes e tente novamente.'])` SEM chamar `Auth::attempt` (CT-04: 422, nunca 429), com comentário `// RF-26: login_failed recorded here by T16`; (4) `attempt` false → `hitLogin` e mensagem existente `E-mail ou senha inválidos.`; (5) sucesso → `clearLogin` (ambas as chaves, RF-10) → `Session::regenerate()` → redirect. Senha nunca vai ao limiter/log/cache.
      Cobre: RF-09, RF-10, RF-12, UI-02, CT-04, RNF-11
      Acceptance criteria: `grep -c 'normalizeEmail' app/Livewire/Auth/LoginForm.php` ≥ 1 e a chamada ocorre antes de `$this->validate()`; 5 senhas erradas → 6.ª com senha correta recusada, `Auth::check()` false, `assertHasErrors(['email'])`; 20 erradas em 5 IPs (4 por IP) → 21.ª correta de um 6.º IP recusada; `Carbon::setTestNow(+61 s)` → autentica; `+15 min +1 s` → autentica; sucesso seguido de 5 erradas conta do zero; variantes de caixa/espaço compartilham o contador (trip em 5); texto do erro idêntico para e-mail existente e inexistente; store `array` (via reflexão em `ArrayStore::$storage`) sem chave/valor igual à senha; `LoginTest` e `LoginFormTest` verdes.
      Testes: `tests/Feature/Auth/LoginRateLimitTest.php` (helper `actingFromIp` em `app('request')->server->set('REMOTE_ADDR', …)`; se `Livewire::test` não refletir o IP, usar o transporte HTTP `/livewire/update` com `withServerVariables` como em `EnsureUserIsActiveTest.php:37`); `php artisan test --compact tests/Feature/Auth/LoginRateLimitTest.php tests/Feature/Auth/LoginTest.php tests/Feature/Livewire/LoginFormTest.php`
- [ ] T09 — Recovery-request limiters in `ForgotPassword::sendResetLink` (UI-03)
      Arquivos: `app/Livewire/Auth/ForgotPassword.php`; `tests/Feature/Auth/PasswordRecoveryRateLimitTest.php` (novo)
      Mudança: normalizar `$this->email` antes de `validate()`; `$ip = request()->ip()`; `if ($limiter->tooManyRecoveryAttempts($email, $ip)) { $limiter->hitRecovery($email, $ip); $this->sent = true; return; }` (toda submissão conta — RF-11); senão `hitRecovery` → `Password::broker('users')->sendResetLink([...])` → `$this->sent = true`. Mesmo estado fixo de confirmação em todo desfecho (UI-03).
      Cobre: RF-11, RF-12, UI-03, RNF-11
      Acceptance criteria: `grep -c 'tooManyRecoveryAttempts' app/Livewire/Auth/ForgotPassword.php` = 1; 4 submissões do mesmo e-mail ativo de um IP → exatamente 1 notificação (`Notification::fake()`), HTML da 4.ª byte-idêntico ao da 1.ª (helper próprio no estilo de `PasswordRecoveryRequestTest.php:90`); 6 e-mails desconhecidos de um IP → 7.ª (e-mail ativo válido) não envia nada; variantes de caixa/espaço compartilham o contador email+IP (trip em 3); `PasswordRecoveryRequestTest`, `PasswordResetTest`, `FirstAccessInviteTest` verdes.
      Testes: `tests/Feature/Auth/PasswordRecoveryRateLimitTest.php`; `php artisan test --compact tests/Feature/Auth/PasswordRecoveryRateLimitTest.php tests/Feature/Auth/PasswordRecoveryRequestTest.php tests/Feature/Auth/PasswordResetTest.php tests/Feature/Auth/FirstAccessInviteTest.php`

## Phase 4: Fase D — Invalidação de sessões após troca de senha

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-14..RF-18, D-09, D-10, R-06)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T10 — `AuthenticateSession` on the `web` group (RF-14/RF-15/RF-18)
      Arquivos: `bootstrap/app.php`; `tests/Feature/Auth/SessionInvalidationOnPasswordChangeTest.php` (novo)
      Mudança: dentro de `withMiddleware`, `$middleware->web(append: [\Illuminate\Session\Middleware\AuthenticateSession::class]);` com comentário citando RF-14, R-06 (sessões pré-existentes em produção recebem o hash na 1.ª requisição e não são cortadas) e que nenhum `logoutOtherDevices` é adicionado ao fluxo guest de reset. Nada mais muda (`trustProxies`, aliases intactos — RF-31).
      Cobre: RF-14, RF-15, RF-18, CT-06
      Acceptance criteria: `grep -c 'AuthenticateSession::class' bootstrap/app.php` = 1 e `grep -c "trustProxies(at: '\*')" bootstrap/app.php` = 1; teste (docblock nomeia a simulação com `SESSION_DRIVER=array`: duas requisições com `withSession(['password_hash_web' => $hashAntigo])` + `actingAs`, `$hashAntigo` lido de `session('password_hash_web')` após um GET autenticado antes do reset): após reset via `ResetPassword` com token de `Password::broker('users')->createToken($user)`, GET `/obra/pedidos` da sessão A → 302 `/login` e `Auth::check()` false; sessão B → idem; update Livewire de snapshot pré-reset → recusado; novo login com a senha nova → 200; mesmo para `AcceptInvite` (broker `invites`); `remember_token` difere antes/depois (RF-15); sessão sem hash armazenado não é cortada e recebe o hash (R-06); `LoginTest` "logout terminates the authenticated session" verde (RF-18).
      Testes: `tests/Feature/Auth/SessionInvalidationOnPasswordChangeTest.php`; `php artisan test --compact tests/Feature/Auth tests/Feature/Livewire/LoginFormTest.php tests/Feature/LivewireSmokeTest.php`
- [ ] T11 — Proofs: deactivation cut and role change via the administrative flow
      Arquivos: `tests/Feature/Auth/SessionAfterAdministrativeChangesTest.php` (novo)
      Mudança: somente teste; docblock cita RF-16, RF-17, D-10. (a) usuário obra com sessão viva (`actingAs` + GET 200); ator `gestao` executa `SetUserActiveAction::execute($gestao, $user, false)` (não update cru); próximo GET `/obra/pedidos` → 302 `/login` com `session('status') === EnsureUserIsActive::DEACTIVATED_MESSAGE`, `Auth::check()` false; update Livewire do snapshot aberto → redirect (padrão `livewireRefresh` de `EnsureUserIsActiveTest.php:30-45` com helper de nome próprio). (b) usuário `gestao` com GET `/gestao/dashboard` 200; outro `gestao` executa `UpdateUserAction` rebaixando-o a `obra` (≥1 obra); mesma sessão: `/gestao/dashboard` → 403 (não 302), `Auth::check()` true; `/home` → redirect `route('obra.pedidos.index')`; `/obra/pedidos` → 200. (c) `suprimentos` rebaixado a `obra` → `UpdatePedidoStatusAction::execute` → `AuthorizationException`; `KanbanBoard::moveViaControl` → `AuthorizationException`.
      Cobre: RF-16 (comportamento), RF-17, AC-F12, AC-F13
      Acceptance criteria: arquivo existe com docblock contendo `D-10`; cenários (a)(b)(c) passam com asserções em status HTTP / classe de exceção / `Auth::check()`; `EnsureUserIsActiveTest` TC-20 e `RoleGatesTest` verdes.
      Testes: `php artisan test --compact tests/Feature/Auth/SessionAfterAdministrativeChangesTest.php tests/Feature/Auth/EnsureUserIsActiveTest.php tests/Feature/Authorization/RoleGatesTest.php`

## Phase 5: Fase E — Trilha administrativa append-only

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-19..RF-25, CT-02, D-03, RNF-04, RNF-10)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T12 — `user_admin_events` migration, `UserAdminEvent` model, enum, policy, factory
      Arquivos: `database/migrations/2026_09_21_000001_create_user_admin_events_table.php` (novo via `make:migration`, timestamp posterior a `2026_09_18_230919`); `app/Models/UserAdminEvent.php` + `database/factories/UserAdminEventFactory.php` (novos via `make:model UserAdminEvent --factory`); `app/Enums/UserAdminAction.php` (novo via `make:enum --string`); `app/Policies/UserAdminEventPolicy.php` (novo via `make:policy --model=UserAdminEvent`); `tests/Unit/Models/UserAdminEventImmutabilityTest.php` (novo); `tests/Feature/MigrationSchemaTest.php` (acrescentar)
      Mudança: schema (CT-02, nomes espelhando `pedido_events`): `id`; `actor_id` `foreignId->constrained('users')->restrictOnDelete()`; `target_id` idem; `action` `string(40)`; `before` `json` nullable; `after` `json` nullable; `created_at` `useCurrent()`; SEM `updated_at`; índices `(target_id, created_at)` e `(actor_id, created_at)` (RF-25); `down()` dropa. Model: `const UPDATED_AT = null`; `#[Fillable(['actor_id','target_id','action','before','after'])]`; casts `action` → `UserAdminAction`, `before`/`after` → `array`; `booted()` `updating`/`deleting` lançam `LogicException` PT-BR; relações `actor()`, `target()`. Enum exatamente: `UserCreated='user_created'`, `UserUpdated`, `RoleChanged`, `ObraAccessChanged`, `UserActivated`, `UserDeactivated`, `AccessLinkSent`, `AccessLinkResent`. Policy `update`/`delete` → `false`. Factory com ator/alvo `User::factory()->gestao()`.
      Cobre: RF-23, RF-24, RF-25, CT-02, RNF-04
      Acceptance criteria: `ls database/migrations | wc -l` = 15 e `git diff --name-status -- database/migrations/` mostra só `A`; `grep -c 'UPDATED_AT = null' app/Models/UserAdminEvent.php` = 1; `grep -c 'case ' app/Enums/UserAdminAction.php` = 8; update/delete via Eloquent lançam `LogicException`; policy nega update/delete para obra/suprimentos/gestao; `MigrationSchemaTest` prova colunas, ausência de `updated_at`, ambos os índices e ambas as FKs com `on_delete = restrict`, e nenhuma coluna chamada `password|password_hash|remember_token|token|secret|api_key|session_id|cookie|authorization`; `FreshMigrationTest` e `MassAssignmentTest` verdes.
      Testes: `tests/Unit/Models/UserAdminEventImmutabilityTest.php` (espelho de `PedidoEventImmutabilityTest`), acréscimos em `tests/Feature/MigrationSchemaTest.php`; `php artisan test --compact tests/Unit/Models/UserAdminEventImmutabilityTest.php tests/Feature/MigrationSchemaTest.php tests/Feature/FreshMigrationTest.php tests/Feature/Security/MassAssignmentTest.php`
- [ ] T13 — `UserAdminAuditRecorder` + audit inside `CreateUserAction`, `UpdateUserAction`, `SetUserActiveAction`
      Arquivos: `app/Services/UserAdminAuditRecorder.php` (novo via `make:class`); `app/Actions/Usuarios/CreateUserAction.php`, `app/Actions/Usuarios/UpdateUserAction.php`, `app/Actions/Usuarios/SetUserActiveAction.php`; `tests/Feature/Actions/Usuarios/UserAdminAuditTest.php` (novo)
      Mudança: recorder com `public const WHITELIST = ['name','email','role','is_active','obra_ids']`; `record(User $actor, User $target, UserAdminAction $action, ?array $before, ?array $after): UserAdminEvent` lança `LogicException` se alguma chave estiver fora da whitelist (RF-21) e então `UserAdminEvent::query()->create()`; `snapshot(User): array{name, email, role, is_active, obra_ids}` (`role` = slug, `obra_ids` ints ordenados). Nunca toca senha/`remember_token`. Actions (injeção no construtor): `CreateUserAction` — dentro da transação existente, após `sync`, `user_created` com `before = null`, `after = snapshot` (5 chaves). `UpdateUserAction` — dentro da transação, `$before = snapshot($target)` antes do `update()`, `$after = snapshot($target->fresh())` após sync/detach; emite `user_updated` só com chaves alteradas entre `name`,`email`; `role_changed` `{role}` se `role_id` mudou; `obra_access_changed` `{obra_ids}` se o conjunto ordenado mudou (inclui detach ao sair de `obra`); dados idênticos → nenhuma linha. `SetUserActiveAction` — envolver em `DB::transaction`; emitir `user_activated`/`user_deactivated` `{is_active}` só se o valor anterior difere; no-op → nada.
      Cobre: RF-19, RF-20, RF-21, RF-22, RNF-10, AC-F19, AC-F20
      Acceptance criteria: `grep -c 'DB::transaction' app/Actions/Usuarios/SetUserActiveAction.php` = 1; `grep -c 'UserAdminAuditRecorder' app/Actions/Usuarios/CreateUserAction.php app/Actions/Usuarios/UpdateUserAction.php app/Actions/Usuarios/SetUserActiveAction.php` ≥ 1 em cada; matriz RF-20 comprovada (uma linha por aspecto alterado, no-ops sem linha, `UpdateUserAction` alterando nome+papel+obras emite 3 linhas com `actor_id`/`target_id` corretos); `array_keys(before ?? [])`/`after` ⊆ whitelist em toda linha e `user_created.after` tem exatamente as 5 chaves; falha forçada em `UserAdminEvent::creating` deixa `users`/`obra_profile`/`is_active` inalterados nas 3 Actions; `CreateUserActionTest`, `UpdateUserActionTest`, `SetUserActiveActionTest`, `GestaoLockoutGuardTest`, `UsuariosFormTest`, `UsuariosIndexTest` verdes.
      Testes: `tests/Feature/Actions/Usuarios/UserAdminAuditTest.php` — matriz de slugs, whitelist, no-ops, falha forçada atômica; `php artisan test --compact tests/Feature/Actions/Usuarios tests/Feature/Livewire/UsuariosFormTest.php tests/Feature/Livewire/UsuariosIndexTest.php tests/Feature/Authorization/UserPolicyTest.php`
- [ ] T14 — `SendAccessLinkAction` explicit `bool $resend` + `access_link_sent`/`access_link_resent` outside transactions (D-03)
      Arquivos: `app/Actions/Usuarios/SendAccessLinkAction.php`, `app/Livewire/Gestao/Usuarios/Index.php`, `app/Actions/Usuarios/CreateUserAction.php`; `tests/Feature/Actions/Usuarios/SendAccessLinkActionTest.php`, `tests/Feature/Actions/Usuarios/UserAdminAuditTest.php`, `tests/Feature/Livewire/UsuariosIndexTest.php` (acrescentar)
      Mudança: `execute(User $actor, User $target, bool $resend = false): string`; após o broker, `if ($status === Password::RESET_LINK_SENT)` → `record($actor, $target, $resend ? AccessLinkResent : AccessLinkSent, null, null)` fora de qualquer transação, falha propaga (D-03). `Index::sendAccessLink` → `$action->execute(Auth::user(), $user, resend: true)`. `CreateUserAction::sendInviteAfterCommit` mantém o default (`resend: false`); o try/catch existente converte falha do recorder em `invite_sent = false` + `report()`. Nunca inferir sent/resent por estado/idade do token. Docblocks citam RF-19/RF-20/D-03.
      Cobre: RF-19, RF-20, RNF-10
      Acceptance criteria: `grep -c 'bool \$resend = false' app/Actions/Usuarios/SendAccessLinkAction.php` = 1 e `grep -c 'resend: true' app/Livewire/Gestao/Usuarios/Index.php` = 1; chamada default → `access_link_sent`; `resend: true` → `access_link_resent`; `RESET_THROTTLED` e `INVALID_USER` → nenhuma linha `access_link_*`; falha forçada do recorder no caminho de `CreateUserAction` → usuário existe, `user_created` presente, `invite_sent === false`, nenhuma `access_link_*`, exceção reportada (`Exceptions::fake()->assertReported`); botão de reenvio no Index emite `access_link_resent`; `SendAccessLinkActionTest` e `Notifications` verdes.
      Testes: acréscimos em `SendAccessLinkActionTest`, `UserAdminAuditTest`, `UsuariosIndexTest`; `php artisan test --compact tests/Feature/Actions/Usuarios tests/Feature/Livewire/UsuariosIndexTest.php tests/Feature/Notifications`

## Phase 6: Fase F — Trilha de autenticação, `session_revoked` e `demo:reset`

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-09 gravação, RF-14/RF-16 eventos, RF-23/RF-24, RF-26..RF-29, CT-03, D-01, D-04, D-05, D-07, D-08, D-09, D-11)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T15 — `authentication_events` migration, `AuthenticationEvent` model, enum, policy, factory
      Arquivos: `database/migrations/2026_09_21_000002_create_authentication_events_table.php` (novo via `make:migration`); `app/Models/AuthenticationEvent.php` + `database/factories/AuthenticationEventFactory.php` (novos via `make:model --factory`); `app/Enums/AuthenticationEventType.php` (novo via `make:enum --string`); `app/Policies/AuthenticationEventPolicy.php` (novo via `make:policy --model=AuthenticationEvent`); `tests/Unit/Models/AuthenticationEventImmutabilityTest.php` (novo); `tests/Feature/MigrationSchemaTest.php` (acrescentar)
      Mudança: schema (CT-03): `id`; `event` `string(32)`; `user_id` `foreignId->nullable()->constrained('users')->restrictOnDelete()`; `email` `string(255)` nullable; `ip` `string(45)` nullable; `user_agent` `string(255)` nullable; `created_at` `useCurrent()`; sem `updated_at`; índices `(user_id, created_at)`, `(email, created_at)`. Model: `UPDATED_AT = null`; `#[Fillable(['event','user_id','email','ip','user_agent'])]`; cast `event` → `AuthenticationEventType`; hooks de imutabilidade (`LogicException` PT-BR); relação `user()`; docblock da classe e da coluna `ip` declarando que o valor é `Request::ip()` atrás de `trustProxies(at: '*')`, derivado de `X-Forwarded-For` e influenciável pelo cliente (D-01), e que não há retenção/expurgo (D-07). Enum exatamente: `LoginSuccess='login_success'`, `LoginFailed`, `Logout`, `PasswordReset`, `PasswordDefined`, `SessionRevoked` — sem `password_changed`. Policy `update`/`delete` false. Factory default `login_success` com usuário.
      Cobre: RF-26, RF-27, RF-28, CT-03, RNF-04
      Acceptance criteria: `ls database/migrations | wc -l` = 16 (só `A` no diff); `grep -c 'case ' app/Enums/AuthenticationEventType.php` = 6 e `grep -rc password_changed app/ | grep -v ':0'` vazio; `grep -c 'X-Forwarded-For' app/Models/AuthenticationEvent.php` ≥ 1; update/delete via Eloquent lançam `LogicException`; policy nega para os 3 papéis; `MigrationSchemaTest` prova colunas, `user_id` nullable com FK `restrict`, ambos os índices, nenhuma coluna proibida (RF-22); `FreshMigrationTest` e `MassAssignmentTest` verdes.
      Testes: `tests/Unit/Models/AuthenticationEventImmutabilityTest.php`, acréscimos em `tests/Feature/MigrationSchemaTest.php`; `php artisan test --compact tests/Unit/Models/AuthenticationEventImmutabilityTest.php tests/Feature/MigrationSchemaTest.php tests/Feature/FreshMigrationTest.php tests/Feature/Security/MassAssignmentTest.php`
- [ ] T16 — `AuthenticationEventRecorder` + `login_success`/`login_failed`/`logout`/`password_reset`/`password_defined`
      Arquivos: `app/Services/AuthenticationEventRecorder.php` (novo via `make:class`); `app/Livewire/Auth/LoginForm.php`, `routes/web.php` (só a closure de `POST /logout`), `app/Livewire/Auth/Concerns/DefinesPasswordFromToken.php`; `tests/Feature/Auth/AuthenticationEventsTest.php` (novo); `tests/Feature/Auth/LoginRateLimitTest.php` (acrescentar)
      Mudança: recorder final (injeta `Request`): `write(AuthenticationEventType, ?User, ?string $email)` privado dentro de `rescue(fn () => AuthenticationEvent::query()->create([...]), report: true)` (RF-29); `email` normalizado (ou o do usuário), `ip = $request->ip()`, `user_agent = mb_substr(preg_replace('/[\x00-\x1F\x7F]/u', '', (string) $request->userAgent()), 0, 255)`; métodos `loginSucceeded(User)`, `loginFailed(string $email, ?User)`, `loggedOut(User)`, `passwordReset(User)`, `passwordDefined(User)`, `sessionRevoked(User)`. Nunca recebe senha/token. Wiring: `LoginForm::authenticate` — ramo do limiter → `loginFailed($email, User::query()->where('email', $email)->first())` antes de lançar (fecha RF-09/D-08); `attempt` false → mesma busca → `loginFailed` (user preenchido para senha errada e conta inativa, `null` para e-mail desconhecido; NÃO usar o evento `Failed`, que traz `user = null` para inativos); sucesso → `loginSucceeded`. Closure `POST /logout` → `app(AuthenticationEventRecorder::class)->loggedOut(Auth::user())` antes de `logout()` (D-09; sem listener genérico de `Logout`). `DefinesPasswordFromToken::definePasswordThroughBroker` — no callback do broker após `save()`, `$broker === 'users' ? passwordReset($user) : passwordDefined($user)` (D-04); `PasswordReset` e rotação de `remember_token` preservados. Respostas HTTP de todos os fluxos inalteradas (RNF-11).
      Cobre: RF-09, RF-26, RF-29, CT-03, AC-F11, AC-F22
      Acceptance criteria: `grep -c 'rescue(' app/Services/AuthenticationEventRecorder.php` ≥ 1; `grep -rn "Events\\\\Logout\|Events\\\\Failed" app/` = 0; `grep -c 'loggedOut' routes/web.php` = 1; `login_success` com `user_id`, e-mail normalizado, `ip` e UA; `login_failed` com `user_id` preenchido para senha errada e conta inativa, `null` para desconhecido, mensagens iguais às de `LoginTest`; tentativa recusada pelo limiter grava `login_failed`; `POST /logout` → exatamente 1 `logout` e 0 `session_revoked`; recuperação grava `password_reset` e não `password_defined`; primeiro acesso grava `password_defined` e não `password_reset`; UA de 1 000 chars com `\x00`/`\n` → ≤ 255 sem caracteres de controle; falha forçada em `AuthenticationEvent::creating` → login ainda redireciona para `/home` com `Auth::check()` true e `Exceptions::fake()->assertReported`; exatamente 1 linha por evento; toda a suíte `tests/Feature/Auth` verde.
      Testes: `tests/Feature/Auth/AuthenticationEventsTest.php`, acréscimo em `tests/Feature/Auth/LoginRateLimitTest.php`; `php artisan test --compact tests/Feature/Auth tests/Feature/Livewire/LoginFormTest.php`
- [ ] T17 — `session_revoked`: deactivation cut + `AuthenticateSession` cut
      Arquivos: `app/Http/Middleware/EnsureUserIsActive.php`; `app/Listeners/RecordSessionRevokedOnCurrentDeviceLogout.php` (novo via `php artisan make:listener RecordSessionRevokedOnCurrentDeviceLogout --event='Illuminate\Auth\Events\CurrentDeviceLogout' --no-interaction`); `tests/Feature/Auth/AuthenticationEventsTest.php`, `tests/Feature/Auth/SessionInvalidationOnPasswordChangeTest.php`, `tests/Feature/Auth/SessionAfterAdministrativeChangesTest.php` (acrescentar)
      Mudança: `EnsureUserIsActive` — injetar `AuthenticationEventRecorder` no construtor; antes de `Auth::guard('web')->logout()` chamar `sessionRevoked($request->user())` (D-09; nunca `logout`). Listener `handle(CurrentDeviceLogout $event)` → se `$event->user instanceof User` → `sessionRevoked($event->user)`; `CurrentDeviceLogout` só é despachado por `SessionGuard::logoutCurrentDevice()` (`SessionGuard.php:703`), chamado só por `AuthenticateSession::logout()` (`AuthenticateSession.php:129`); `Auth::logout()` despacha `Logout` (`SessionGuard.php:664`), logo logout explícito e corte por desativação nunca duplicam. Registro por descoberta automática de `app/Listeners` (sem `withEvents(false)` em `bootstrap/app.php`; `event:cache` do Railpack compatível) — NÃO adicionar `Event::listen` também (linhas duplicadas); docblock declara isso.
      Cobre: RF-14, RF-16, RF-26, D-09
      Acceptance criteria: `grep -c 'sessionRevoked' app/Http/Middleware/EnsureUserIsActive.php` = 1; `ls app/Listeners/RecordSessionRevokedOnCurrentDeviceLogout.php` existe e `grep -c 'CurrentDeviceLogout' app/Providers/AppServiceProvider.php` = 0; desativação via `SetUserActiveAction` seguida de requisição da sessão viva → exatamente 1 `session_revoked` e 0 `logout` para o usuário; duas sessões pré-reset cortadas por `AuthenticateSession` → 2 `session_revoked`, 0 `logout`; rebaixamento de papel → 0 `session_revoked`/0 `logout` (D-10); `POST /logout` → 0 `session_revoked`; cada corte grava exatamente 1 linha; `tests/Feature/Auth` verde.
      Testes: acréscimos em `AuthenticationEventsTest`, `SessionInvalidationOnPasswordChangeTest`, `SessionAfterAdministrativeChangesTest`; `php artisan test --compact tests/Feature/Auth`
- [ ] T18 — `demo:reset` deletes demo-referencing audit rows via `DB::table` (D-05, D-11)
      Arquivos: `app/Console/Commands/ResetDemoData.php`; `tests/Feature/Console/ResetDemoDataAuditTrailsTest.php` (novo); `tests/Feature/Compliance/AuditTrailsAppendOnlyTest.php` (novo)
      Mudança: dentro da `DB::transaction` existente, ANTES de `User::query()->where('is_demo', true)->delete()`: `$demoUserIds = User::query()->where('is_demo', true)->pluck('id'); DB::table('user_admin_events')->where(fn ($q) => $q->whereIn('actor_id', $demoUserIds)->orWhereIn('target_id', $demoUserIds))->delete(); DB::table('authentication_events')->whereIn('user_id', $demoUserIds)->delete();` — só query builder (contorna o guard `deleting` do Eloquent por design, D-11); linhas com `user_id = null` e linhas só de usuários reais nunca são tocadas (D-05). Docblock atualizado (ordem por FK `restrictOnDelete`).
      Cobre: RF-23, RF-24, RF-28, D-05, D-11
      Acceptance criteria: `grep -c "DB::table('user_admin_events')" app/Console/Commands/ResetDemoData.php` = 1 e `grep -c "DB::table('authentication_events')" app/Console/Commands/ResetDemoData.php` = 1, ambos antes do delete de `User`; fixture (i) só demo, (ii) ator real → alvo demo, (iii) ator demo → alvo real, (iv) só reais, (v) `login_failed` com `user_id = null` e e-mail demo → `demo:reset --force` sucede, (i)(ii)(iii) somem, (iv)(v) permanecem; `User::delete()` de usuário real com linhas de auditoria lança `QueryException`; `AuditTrailsAppendOnlyTest` prova que nenhuma instrução em `app/` começando por `UserAdminEvent::`/`AuthenticationEvent::` contém `->update(`/`->delete(`, que `ResetDemoData.php` usa só `DB::table(` para as trilhas e que nenhuma rota registrada tem `audit`/`events`/`trilha` no uri/nome; `ResetDemoDataTest` verde.
      Testes: `tests/Feature/Console/ResetDemoDataAuditTrailsTest.php`, `tests/Feature/Compliance/AuditTrailsAppendOnlyTest.php`; `php artisan test --compact tests/Feature/Console tests/Feature/Compliance/AuditTrailsAppendOnlyTest.php tests/Feature/Seeders`

## Phase 7: Fase G — Suíte adversarial de autorização

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-30 G-01..G-14, RF-22, RNF-03, RNF-09, D-12)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T19 — Adversarial suite: cross-obra and cross-role (G-01..G-08, G-14)
      Arquivos: `tests/Feature/Security/Adversarial/CrossObraTest.php`, `tests/Feature/Security/Adversarial/CrossRoleTest.php` (novos)
      Mudança: somente teste (D-12), cada nome iniciando pelo id (`test('G-01 …')`). `CrossObraTest`: G-01 obra A lista `/obra/pedidos` → `assertDontSee(pedidoB->code)`; G-02 GET `/obra/pedidos/{pedidoB}` → 403; G-03 `Livewire::test(Obra\PedidoDetalhe::class, ['pedido' => $pedidoB])` → `AuthorizationException`; G-04 `NovaSolicitacao` com `obra_id` B forjado → `assertHasErrors(['obra_id'])`, sem insert; G-14 obra inativa via `CreatePedidoAction::execute` → `ValidationException` `obra_id`, sem insert, sequência intocada. `CrossRoleTest`: G-05 obra → toda rota `suprimentos.*` (resolvendo `{pedido}`) → 403; G-06 obra → toda rota `gestao.*` (resolvendo `{user}`) → 403; G-07 suprimentos → `/gestao/usuarios`, `/gestao/usuarios/novo`, `/gestao/usuarios/{id}/editar` → 403 e cada uma das 4 Actions de `Actions/Usuarios` chamada diretamente → `AuthorizationException`; G-08 gestao → as 5 Actions operacionais diretamente → `AuthorizationException` e via `Livewire::test(Suprimentos\PedidoDetalhe::class)->call('updateStatus'|'updateResponsavel'|'updatePrioridade'|'updatePrevisao'|'cancelarPedido')` e `KanbanBoard::moveCard`/`moveViaControl` → `AuthorizationException`. Estado do banco inalterado após cada negação; nunca afirmar sobre menus.
      Cobre: RF-30 (G-01..G-08, G-14), AC-F01..AC-F10, AC-F17, AC-F18
      Acceptance criteria: `ls tests/Feature/Security/Adversarial/` contém `CrossObraTest.php` e `CrossRoleTest.php`; `grep -hoE "test\('G-[0-9]{2}" tests/Feature/Security/Adversarial/*.php | sort -u` inclui G-01..G-08 e G-14; `php artisan test --compact --filter=Adversarial` verde e `--filter='G-0[1-4]'` executa só esses; toda asserção é status HTTP, classe de exceção ou estado do banco.
      Testes: `php artisan test --compact --filter=Adversarial && php artisan test --compact --filter='G-0[1-4]'`
- [ ] T20 — Adversarial suite: session, role, rate limit, audit, no-Global-Scope/no-RLS (G-09..G-13, RNF-03)
      Arquivos: `tests/Feature/Security/Adversarial/SessionAndRoleTest.php`, `tests/Feature/Security/Adversarial/RateLimitTest.php`, `tests/Feature/Security/Adversarial/AuditTrailTest.php`, `tests/Feature/Security/Adversarial/NoGlobalScopeNoRlsTest.php` (novos)
      Mudança: somente teste. G-09 rebaixamento via `UpdateUserAction` → mesma sessão 403 na área antiga, `Auth::check()` true, `/home` redireciona, sem `session_revoked`/`logout`; G-10 desativação via `SetUserActiveAction` → sessão viva redirecionada ao login + exatamente 1 `session_revoked`; G-11 duas sessões pré-reset → ambas recusadas + 2 `session_revoked`; G-12 os 4 limitadores disparam exatamente em 5/60 s, 20/15 min, 3/60 s, 6/60 s, respostas byte-idênticas para e-mail existente/desconhecido, `login_failed` para tentativas barradas, nenhum e-mail quando a recuperação dispara; G-13 fluxo completo criar usuário + convite + editar + desativar → linhas com ator/alvo/ação/before/after corretos e varredura RF-22: `Notification::fake()` captura o token cru; toda coluna de toda linha das 2 trilhas (`DB::table()->get()` → `json_encode` → `stripos`) não contém a senha em claro usada, o hash `users.password`, `remember_token`, o token cru, `password_reset_tokens.token` nem o id de sessão; `NoGlobalScopeNoRlsTest`: `app/Models/Pedido.php` sem `addGlobalScope`/`ScopedBy`; `database/` sem `POLICY|ROW LEVEL SECURITY`; `app/` sem `password_changed`. Duplicar cenários das Fases 3–6 é intencional.
      Cobre: RF-30 (G-09..G-13), RF-22, RF-27, RNF-03, RNF-09, AC-F12..AC-F16, AC-F19..AC-F22
      Acceptance criteria: os 4 arquivos existem; `grep -hoE "test\('G-[0-9]{2}" tests/Feature/Security/Adversarial/*.php | sort -u | wc -l` ≥ 14 cobrindo G-01..G-14; `php artisan test --compact --filter=Adversarial` verde; a varredura RF-22 passa nas duas trilhas; `grep -cE 'addGlobalScope|ScopedBy' app/Models/Pedido.php` = 0 e `grep -rciE 'ROW LEVEL SECURITY|CREATE POLICY' database/ | grep -v ':0'` vazio.
      Testes: `php artisan test --compact --filter=Adversarial`

## Phase 8: Fase H — Revisão de configuração de produção no código

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-31, AC-N10, AC-N11)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T21 — Production configuration compliance test (RF-31)
      Arquivos: `tests/Feature/Compliance/ProductionConfigTest.php` (novo); `.env.example` (só o bloco de comentário do cabeçalho, e só se faltar uma linha exigida — o cabeçalho já declara `APP_ENV=production`, `APP_DEBUG=false`, `SESSION_SECURE_COOKIE=true`; adicionar "(sob HTTPS)" à linha de `SESSION_SECURE_COOKIE` se ausente; valores locais intocados)
      Mudança: asserções sem ler `.env` real nem Railway: regex sobre o fonte de `config/session.php` — `'driver' => env('SESSION_DRIVER', 'database')`, `'lifetime' => (int) env('SESSION_LIFETIME', 120)`, `'secure' => env('SESSION_SECURE_COOKIE')`, `'http_only' => env('SESSION_HTTP_ONLY', true)`, `'same_site' => env('SESSION_SAME_SITE', 'lax')`; `config('auth.passwords.users')` expire 60 / throttle 60 e `config('auth.passwords.invites')` expire 4320 / throttle 60; fonte de `bootstrap/app.php` contém `trustProxies(at: '*')`, `'active' => EnsureUserIsActive::class` e `AuthenticateSession::class`; cabeçalho de `.env.example` contém `APP_ENV=production`, `APP_DEBUG=false`, `SESSION_SECURE_COOKIE=true` enquanto `APP_ENV=local` e `APP_DEBUG=true` permanecem como valores locais; varredura de `app/`: nenhuma chamada `Log::`/`report(` cujo argumento mencione `password`, `token`, `->all()` ou payload de `$request`. Docblock lista cada item checado para o relatório "Configuration review".
      Cobre: RF-31, AC-N10, AC-N11
      Acceptance criteria: arquivo existe e passa; `git diff -- .env.example` vazio ou limitado a linhas iniciadas por `#`; `EnvExampleTest`, `NoCommittedSecretsTest`, `MailTransportTest` verdes; `grep -c '^APP_ENV=local$' .env.example` = 1 e `grep -c '^APP_DEBUG=true$' .env.example` = 1.
      Testes: `php artisan test --compact tests/Feature/Compliance`

## Phase 9: Fase I — Gates completos, auditoria de dependências e relatório final

Antes de implementar, leia:
1. `.spec/features/security-hardening-production/SPEC.md` — requisitos RIGID que esta fase cobre (RF-32, RF-33, RNF-01, RNF-02, RNF-04..RNF-06, RNF-08)
2. `.spec/features/security-hardening-production/PLAN.md` — decomposição completa, dependências e riscos

- [ ] T22 — Full quality gates and dependency audit (description §18, RF-32, RNF-01..RNF-06, RNF-08)
      Arquivos: nenhum modificado (Pint pode reformatar arquivos já tocados — commitar só esses)
      Mudança: executar e registrar saídas literais: (1) `php artisan test --compact` → 0 falhas; (2) `vendor/bin/pint --dirty --format agent` → sem alterações restantes; (3) `npm run build` → concluído; (4) `composer audit` → tabela por advisory (pacote, severidade, versão afetada, versão corrigida se informada, impacto provável) — NUNCA `composer update`; (5) `npm audit` → mesma tabela — NUNCA `npm audit fix`/`npm update`; (6) `git diff --stat build/v0-demo-laravel...HEAD -- composer.json composer.lock package.json package-lock.json` → vazio; (7) `git diff --name-status build/v0-demo-laravel...HEAD -- database/migrations/` → só `A`, ambas datadas após `2026_09_18_230919`; (8) `git diff --stat build/v0-demo-laravel...HEAD -- tests/` → sem `D`; contagem de `test(`/asserções de cada arquivo de teste pré-existente não decrescente; (9) varredura de segredos: `NoCommittedSecretsTest` + `git diff build/v0-demo-laravel...HEAD | grep -inE 'BEGIN (RSA|OPENSSH|PRIVATE)|re_[A-Za-z0-9]{20,}|eyJ[A-Za-z0-9_-]{20,}'` → vazio; `git status --porcelain | grep -E '^\?\? \.env'` → vazio; (10) checagem estática PHP 8.4 (runtime local 8.5.4): `grep -rnE '\|>|#\[\\?NoDiscard\]|\barray_(first|last)\(|\bclone\(' app/ database/ routes/ tests/` → 0 e `composer.json` `php: ^8.4` inalterado; (11) `git rev-parse build/v0-demo-laravel` = `788740e` e nenhum comando Railway executado.
      Cobre: RF-32, RNF-01, RNF-02, RNF-04, RNF-05, RNF-06, RNF-08, AC-F23, AC-F24, AC-N10, AC-N11
      Acceptance criteria: log da fase contém as 11 saídas; `php artisan test --compact` termina com 0 failures; `npm run build` exit 0; `git diff --stat build/v0-demo-laravel...HEAD -- composer.lock package-lock.json` vazio; `git diff --name-status build/v0-demo-laravel...HEAD -- database/migrations/` mostra exatamente 2 linhas `A`; grep de sintaxe 8.5 = 0; `git rev-parse build/v0-demo-laravel` inalterado.
      Testes: suíte completa `php artisan test --compact`
- [ ] T23 — Final report §23 A–P (RF-33)
      Arquivos: nenhum commitado (mensagem de encerramento da execução; arquivo só se o desenvolvedor pedir)
      Mudança: produzir o relatório com os 16 itens: A resumo; B arquivos criados; C arquivos modificados; D migrations criadas; E controles de segurança adicionados; F testes adversariais adicionados; G resultado dos testes focados por fase; H saída de `php artisan test --compact`; I saída de `npm run build`; J tabela de `composer audit`; K tabela de `npm audit`; L decisões tomadas (citar D-01..D-12 e a convenção `#[Scope]`); M decisões pendentes (R-02 retenção/LGPD; vulnerabilidades que exijam atualização); N riscos residuais (R-01..R-06 e itens `[UNVERIFIED]` do PLAN resolvidos ou não); O confirmação explícita de zero alteração em produção/Railway e `build/v0-demo-laravel` intocada; P confirmação explícita de nenhum segredo commitado. Incluir a seção "Configuration review" (RF-31, veredito por item, sem valores secretos) e a declaração da checagem estática RNF-01.
      Cobre: RF-33, RF-31, RF-32, AC-N11
      Acceptance criteria: a mensagem de encerramento contém os 16 cabeçalhos A..P, a seção "Configuration review" e cita D-01..D-12 e R-01..R-06; `git status --porcelain` limpo além dos commits pretendidos; nenhum arquivo de relatório novo em `git status`.
      Testes: nenhum (documental) — validar por inspeção da mensagem e `git status --porcelain`
